[
  {
    "Command": [],
    "Constant": [],
    "Constructor": [],
    "Deprecated": [],
    "Field": [],
    "Function": [],
    "Method": [
      {
        "def": "DeminimizeWindow:abortYabai() -> self",
        "desc": "Kills any yabai commands in flight and abandons whatever they were part of.",
        "doc": "Kills any yabai commands in flight and abandons whatever they were part of.\n\nParameters:\n * None\n\nReturns:\n * The DeminimizeWindow object\n\nCalled from `DeminimizeWindow:stop()`. Each job is marked settled *before* its process is signalled: terminating a task causes a completion callback rather than cancelling one, so without the flag a config reload would answer into a half-torn-down Spoon and walk the ladder on into a `--focus`.",
        "name": "abortYabai",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The DeminimizeWindow object",
          "",
          "Called from `DeminimizeWindow:stop()`. Each job is marked settled *before* its process is signalled: terminating a task causes a completion callback rather than cancelling one, so without the flag a config reload would answer into a half-torn-down Spoon and walk the ladder on into a `--focus`."
        ],
        "signature": "DeminimizeWindow:abortYabai() -> self",
        "stripped_doc": [
          "Kills any yabai commands in flight and abandons whatever they were part of.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "DeminimizeWindow:collect(done) -> self",
        "desc": "Builds the list of restorable windows and hands it to a callback.",
        "doc": "Builds the list of restorable windows and hands it to a callback.\n\nParameters:\n * done - A function called as `done(items, space, err)`. `items` is a sorted list of tables with `winId`, `appName`, `title` and `minimized` keys; `space` describes the Space they would be restored to; both are nil when `err` is set\n\nReturns:\n * The DeminimizeWindow object\n\nUnions two sources by window id, the same `CGWindowID` in both, so the join needs no guessing. yabai is authoritative about window state and Spaces; Accessibility contributes the `hs.window` the yabai-free rungs need.\nEither source alone yields a usable entry: a window only yabai can see is restorable by yabai only, and one only Accessibility can see cannot be placed with confidence.",
        "name": "collect",
        "notes": [],
        "parameters": [
          " * done - A function called as `done(items, space, err)`. `items` is a sorted list of tables with `winId`, `appName`, `title` and `minimized` keys; `space` describes the Space they would be restored to; both are nil when `err` is set",
          ""
        ],
        "returns": [
          " * The DeminimizeWindow object",
          "",
          "Unions two sources by window id, the same `CGWindowID` in both, so the join needs no guessing. yabai is authoritative about window state and Spaces; Accessibility contributes the `hs.window` the yabai-free rungs need.",
          "Either source alone yields a usable entry: a window only yabai can see is restorable by yabai only, and one only Accessibility can see cannot be placed with confidence."
        ],
        "signature": "DeminimizeWindow:collect(done) -> self",
        "stripped_doc": [
          "Builds the list of restorable windows and hands it to a callback.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "DeminimizeWindow:restoreById(winId) -> self",
        "desc": "Restores one minimized window by its window id, onto the current Space.",
        "doc": "Restores one minimized window by its window id, onto the current Space.\n\nParameters:\n * winId - The window id, as returned in the `winId` field of `DeminimizeWindow:collect()`\n\nReturns:\n * The DeminimizeWindow object\n\nThe id must have come from a `DeminimizeWindow:collect()` pass, since everything else known about the window is looked up in the map that call builds.\nThe target Space and the window's state are re-read here rather than taken from the list, because a chooser can sit open across a Space change.",
        "name": "restoreById",
        "notes": [],
        "parameters": [
          " * winId - The window id, as returned in the `winId` field of `DeminimizeWindow:collect()`",
          ""
        ],
        "returns": [
          " * The DeminimizeWindow object",
          "",
          "The id must have come from a `DeminimizeWindow:collect()` pass, since everything else known about the window is looked up in the map that call builds.",
          "The target Space and the window's state are re-read here rather than taken from the list, because a chooser can sit open across a Space change."
        ],
        "signature": "DeminimizeWindow:restoreById(winId) -> self",
        "stripped_doc": [
          "Restores one minimized window by its window id, onto the current Space.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "DeminimizeWindow:restore() -> self",
        "desc": "Restores a minimized window, asking which one only when there is a choice.",
        "doc": "Restores a minimized window, asking which one only when there is a choice.\n\nParameters:\n * None\n\nReturns:\n * The DeminimizeWindow object\n\nWith no minimized windows this shows a brief alert, with exactly one it restores it outright, with more it opens the chooser. See `DeminimizeWindow.skipChooserForSingle`.\nThis is the method `DeminimizeWindow:bindHotkeys()` binds; pressing the hotkey while the chooser is open closes it again.",
        "name": "restore",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The DeminimizeWindow object",
          "",
          "With no minimized windows this shows a brief alert, with exactly one it restores it outright, with more it opens the chooser. See `DeminimizeWindow.skipChooserForSingle`.",
          "This is the method `DeminimizeWindow:bindHotkeys()` binds; pressing the hotkey while the chooser is open closes it again."
        ],
        "signature": "DeminimizeWindow:restore() -> self",
        "stripped_doc": [
          "Restores a minimized window, asking which one only when there is a choice.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "DeminimizeWindow:show() -> self",
        "desc": "Opens the chooser, whatever the number of minimized windows.",
        "doc": "Opens the chooser, whatever the number of minimized windows.\n\nParameters:\n * None\n\nReturns:\n * The DeminimizeWindow object\n\nUnlike `DeminimizeWindow:restore()` this never restores anything by itself, so it is the method to bind if you would always rather see the list first.",
        "name": "show",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The DeminimizeWindow object",
          "",
          "Unlike `DeminimizeWindow:restore()` this never restores anything by itself, so it is the method to bind if you would always rather see the list first."
        ],
        "signature": "DeminimizeWindow:show() -> self",
        "stripped_doc": [
          "Opens the chooser, whatever the number of minimized windows.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "DeminimizeWindow:hide() -> self",
        "desc": "Closes the chooser if it is open.",
        "doc": "Closes the chooser if it is open.\n\nParameters:\n * None\n\nReturns:\n * The DeminimizeWindow object",
        "name": "hide",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The DeminimizeWindow object"
        ],
        "signature": "DeminimizeWindow:hide() -> self",
        "stripped_doc": [
          "Closes the chooser if it is open.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "DeminimizeWindow:init() -> self",
        "desc": "Prepares the Spoon. Called automatically by `hs.loadSpoon()`.",
        "doc": "Prepares the Spoon. Called automatically by `hs.loadSpoon()`.\n\nParameters:\n * None\n\nReturns:\n * The DeminimizeWindow object\n\nDeliberately starts nothing. The chooser and the menubar item both belong to `DeminimizeWindow:start()`.\nDeliberately empty rather than re-initialising state: `hs.loadSpoon()` reaches `init()` through `require()`, which returns a cached object on a second load, so clearing state here would strand a live chooser.",
        "name": "init",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The DeminimizeWindow object",
          "",
          "Deliberately starts nothing. The chooser and the menubar item both belong to `DeminimizeWindow:start()`.",
          "Deliberately empty rather than re-initialising state: `hs.loadSpoon()` reaches `init()` through `require()`, which returns a cached object on a second load, so clearing state here would strand a live chooser."
        ],
        "signature": "DeminimizeWindow:init() -> self",
        "stripped_doc": [
          "Prepares the Spoon. Called automatically by `hs.loadSpoon()`.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "DeminimizeWindow:start() -> self",
        "desc": "Makes the Spoon ready to restore windows, and adds the menubar item if it is enabled.",
        "doc": "Makes the Spoon ready to restore windows, and adds the menubar item if it is enabled.\n\nParameters:\n * None\n\nReturns:\n * The DeminimizeWindow object\n\nCalling this on an already-started Spoon restarts it cleanly.\nNothing polls and no timer runs while idle. The window list is built on demand, when the hotkey is pressed or the menu is pulled down.\nWarns via `hs.alert` if Accessibility permission has not been granted, since nothing works without it.",
        "name": "start",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The DeminimizeWindow object",
          "",
          "Calling this on an already-started Spoon restarts it cleanly.",
          "Nothing polls and no timer runs while idle. The window list is built on demand, when the hotkey is pressed or the menu is pulled down.",
          "Warns via `hs.alert` if Accessibility permission has not been granted, since nothing works without it."
        ],
        "signature": "DeminimizeWindow:start() -> self",
        "stripped_doc": [
          "Makes the Spoon ready to restore windows, and adds the menubar item if it is enabled.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "DeminimizeWindow:stop() -> self",
        "desc": "Abandons any restore in flight, destroys the chooser and removes the menubar item.",
        "doc": "Abandons any restore in flight, destroys the chooser and removes the menubar item.\n\nParameters:\n * None\n\nReturns:\n * The DeminimizeWindow object\n\nAny hotkeys bound with `DeminimizeWindow:bindHotkeys()` stay bound; rebind or delete them separately if you want them gone.",
        "name": "stop",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The DeminimizeWindow object",
          "",
          "Any hotkeys bound with `DeminimizeWindow:bindHotkeys()` stay bound; rebind or delete them separately if you want them gone."
        ],
        "signature": "DeminimizeWindow:stop() -> self",
        "stripped_doc": [
          "Abandons any restore in flight, destroys the chooser and removes the menubar item.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "DeminimizeWindow:bindHotkeys(mapping) -> self",
        "desc": "Binds hotkeys for DeminimizeWindow.",
        "doc": "Binds hotkeys for DeminimizeWindow.\n\nParameters:\n * mapping - A table containing hotkey modifier/key details for the following items:\n   * restore - Restore a minimized window, showing the chooser only when there is a choice\n   * show - Always show the chooser, whatever the number of minimized windows\n\nReturns:\n * The DeminimizeWindow object\n\nFor example: `spoon.DeminimizeWindow:bindHotkeys({ restore = { { \"cmd\", \"alt\", \"ctrl\" }, \"M\" } })`",
        "name": "bindHotkeys",
        "notes": [],
        "parameters": [
          " * mapping - A table containing hotkey modifier/key details for the following items:",
          "   * restore - Restore a minimized window, showing the chooser only when there is a choice",
          "   * show - Always show the chooser, whatever the number of minimized windows",
          ""
        ],
        "returns": [
          " * The DeminimizeWindow object",
          "",
          "For example: `spoon.DeminimizeWindow:bindHotkeys({ restore = { { \"cmd\", \"alt\", \"ctrl\" }, \"M\" } })`"
        ],
        "signature": "DeminimizeWindow:bindHotkeys(mapping) -> self",
        "stripped_doc": [
          "Binds hotkeys for DeminimizeWindow.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "DeminimizeWindow:status() -> table",
        "desc": "Returns the Spoon's current state, for poking at from the Hammerspoon Console.",
        "doc": "Returns the Spoon's current state, for poking at from the Hammerspoon Console.\n\nParameters:\n * None\n\nReturns:\n * A table with `running`, `menubar`, `chooserVisible`, `restoring`, `yabai`, `yabaiBusy`, `yabaiLastError`, `listed` and `timers` keys\n\n`yabai` is the resolved binary path, or `false` when yabai is off or not installed. It says nothing about whether the yabai *service* is running; only `DeminimizeWindow:diagnose()` answers that.\n`listed` counts the windows in the most recent list, which may be stale; the list is never reused across a press.",
        "name": "status",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * A table with `running`, `menubar`, `chooserVisible`, `restoring`, `yabai`, `yabaiBusy`, `yabaiLastError`, `listed` and `timers` keys",
          "",
          "`yabai` is the resolved binary path, or `false` when yabai is off or not installed. It says nothing about whether the yabai *service* is running; only `DeminimizeWindow:diagnose()` answers that.",
          "`listed` counts the windows in the most recent list, which may be stale; the list is never reused across a press."
        ],
        "signature": "DeminimizeWindow:status() -> table",
        "stripped_doc": [
          "Returns the Spoon's current state, for poking at from the Hammerspoon Console.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "DeminimizeWindow:diagnose([done]) -> self",
        "desc": "Prints what each source can see, and why a window is or is not being offered.",
        "doc": "Prints what each source can see, and why a window is or is not being offered.\n\nParameters:\n * done - An optional function called with the report text once it has been gathered\n\nReturns:\n * The DeminimizeWindow object\n\nAsynchronous, because it asks yabai. The report is printed to the Console when it arrives, so calling this bare from the Console is the normal way to use it.\nThe decisive line is the per-window one: a row tagged only `yabai` has no `hs.window`, so the Accessibility rungs do not apply to it, and a row tagged only `ax` cannot be placed with confidence.",
        "name": "diagnose",
        "notes": [],
        "parameters": [
          " * done - An optional function called with the report text once it has been gathered",
          ""
        ],
        "returns": [
          " * The DeminimizeWindow object",
          "",
          "Asynchronous, because it asks yabai. The report is printed to the Console when it arrives, so calling this bare from the Console is the normal way to use it.",
          "The decisive line is the per-window one: a row tagged only `yabai` has no `hs.window`, so the Accessibility rungs do not apply to it, and a row tagged only `ax` cannot be placed with confidence."
        ],
        "signature": "DeminimizeWindow:diagnose([done]) -> self",
        "stripped_doc": [
          "Prints what each source can see, and why a window is or is not being offered.",
          ""
        ],
        "type": "Method"
      }
    ],
    "Variable": [
      {
        "def": "DeminimizeWindow.logger",
        "desc": "Logger object used within the Spoon. Can be accessed to set the default log level for the messages coming from the Spoon.",
        "doc": "Logger object used within the Spoon. Can be accessed to set the default log level for the messages coming from the Spoon.",
        "name": "logger",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.logger",
        "stripped_doc": [
          "Logger object used within the Spoon. Can be accessed to set the default log level for the messages coming from the Spoon."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.useYabai",
        "desc": "Whether yabai is used to move the restored window onto the current Space. Defaults to `true`.",
        "doc": "Whether yabai is used to move the restored window onto the current Space. Defaults to `true`.\n\nWith this off, or yabai absent, restores go through Accessibility alone, which cannot choose a Space: a window minimized elsewhere reappears there, and the Spoon says so rather than pretending it succeeded.\n`hs.spaces.moveWindowToSpace()` is not an alternative; it has been a silent no-op since macOS 15.",
        "name": "useYabai",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.useYabai",
        "stripped_doc": [
          "Whether yabai is used to move the restored window onto the current Space. Defaults to `true`.",
          "",
          "With this off, or yabai absent, restores go through Accessibility alone, which cannot choose a Space: a window minimized elsewhere reappears there, and the Spoon says so rather than pretending it succeeded.",
          "`hs.spaces.moveWindowToSpace()` is not an alternative; it has been a silent no-op since macOS 15."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.yabaiPath",
        "desc": "Absolute path to the yabai binary, or `nil` to probe the usual locations. Defaults to `nil`.",
        "doc": "Absolute path to the yabai binary, or `nil` to probe the usual locations. Defaults to `nil`.\n\nOnly needed for an installation somewhere unusual. Homebrew on both architectures, nix-darwin and `~/.local/bin` are probed already.",
        "name": "yabaiPath",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.yabaiPath",
        "stripped_doc": [
          "Absolute path to the yabai binary, or `nil` to probe the usual locations. Defaults to `nil`.",
          "",
          "Only needed for an installation somewhere unusual. Homebrew on both architectures, nix-darwin and `~/.local/bin` are probed already."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.yabaiTimeout",
        "desc": "Seconds any single yabai command may take before it is abandoned. Defaults to `1.5`.",
        "doc": "Seconds any single yabai command may take before it is abandoned. Defaults to `1.5`.",
        "name": "yabaiTimeout",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.yabaiTimeout",
        "stripped_doc": [
          "Seconds any single yabai command may take before it is abandoned. Defaults to `1.5`."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.placeTimeout",
        "desc": "Seconds spent confirming that a window really did land on the current Space. Defaults to `0.6`.",
        "doc": "Seconds spent confirming that a window really did land on the current Space. Defaults to `0.6`.\n\nThis is a confirmation window, not a delay. A move that worked is usually visible on the first poll.",
        "name": "placeTimeout",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.placeTimeout",
        "stripped_doc": [
          "Seconds spent confirming that a window really did land on the current Space. Defaults to `0.6`.",
          "",
          "This is a confirmation window, not a delay. A move that worked is usually visible on the first poll."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.revealTimeout",
        "desc": "Seconds spent confirming that a window really did come out of the Dock. Defaults to `1.0`.",
        "doc": "Seconds spent confirming that a window really did come out of the Dock. Defaults to `1.0`.\n\nLonger than `DeminimizeWindow.placeTimeout`: the un-minimize animation alone runs about half a second.",
        "name": "revealTimeout",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.revealTimeout",
        "stripped_doc": [
          "Seconds spent confirming that a window really did come out of the Dock. Defaults to `1.0`.",
          "",
          "Longer than `DeminimizeWindow.placeTimeout`: the un-minimize animation alone runs about half a second."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.pollInterval",
        "desc": "Seconds between checks while confirming a step. Defaults to `0.12`.",
        "doc": "Seconds between checks while confirming a step. Defaults to `0.12`.\n\nEach yabai poll is a subprocess, which is what sets this floor. Checks that can be answered from an `hs.window` run at a quarter of this interval, since they cost nothing but a function call.",
        "name": "pollInterval",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.pollInterval",
        "stripped_doc": [
          "Seconds between checks while confirming a step. Defaults to `0.12`.",
          "",
          "Each yabai poll is a subprocess, which is what sets this floor. Checks that can be answered from an `hs.window` run at a quarter of this interval, since they cost nothing but a function call."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.ladderDeadline",
        "desc": "Seconds after which a restore still in flight is abandoned outright. Defaults to `8`.",
        "doc": "Seconds after which a restore still in flight is abandoned outright. Defaults to `8`.\n\nA backstop for the busy flag rather than a timeout anybody should reach: without it, a restore stranded by an application that never answers would leave the hotkey permanently deaf.",
        "name": "ladderDeadline",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.ladderDeadline",
        "stripped_doc": [
          "Seconds after which a restore still in flight is abandoned outright. Defaults to `8`.",
          "",
          "A backstop for the busy flag rather than a timeout anybody should reach: without it, a restore stranded by an application that never answers would leave the hotkey permanently deaf."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.skipChooserForSingle",
        "desc": "Whether a lone minimized window is restored without showing the chooser. Defaults to `true`.",
        "doc": "Whether a lone minimized window is restored without showing the chooser. Defaults to `true`.",
        "name": "skipChooserForSingle",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.skipChooserForSingle",
        "stripped_doc": [
          "Whether a lone minimized window is restored without showing the chooser. Defaults to `true`."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.focusAfterRestore",
        "desc": "Whether the restored window is focused and raised once it has arrived. Defaults to `true`.",
        "doc": "Whether the restored window is focused and raised once it has arrived. Defaults to `true`.\n\nReached only from success: focusing a window that never moved is what drags the user to its Space, so no failure path arrives here.\n`yabai --deminimize` does not focus, so this is always an explicit extra step.",
        "name": "focusAfterRestore",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.focusAfterRestore",
        "stripped_doc": [
          "Whether the restored window is focused and raised once it has arrived. Defaults to `true`.",
          "",
          "Reached only from success: focusing a window that never moved is what drags the user to its Space, so no failure path arrives here.",
          "`yabai --deminimize` does not focus, so this is always an explicit extra step."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.allowRevealFirst",
        "desc": "Whether the reveal-first rungs of the ladder may run after placing has failed. Defaults to `true`.",
        "doc": "Whether the reveal-first rungs of the ladder may run after placing has failed. Defaults to `true`.\n\nThe only rungs that can pull you to another Space, and they run only once placing has failed, when the choice is between that risk and not restoring the window at all.\nSet to `false` for a Spoon that is structurally incapable of moving you, and that occasionally does nothing instead.",
        "name": "allowRevealFirst",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.allowRevealFirst",
        "stripped_doc": [
          "Whether the reveal-first rungs of the ladder may run after placing has failed. Defaults to `true`.",
          "",
          "The only rungs that can pull you to another Space, and they run only once placing has failed, when the choice is between that risk and not restoring the window at all.",
          "Set to `false` for a Spoon that is structurally incapable of moving you, and that occasionally does nothing instead."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.returnAfterSpaceChange",
        "desc": "Whether to send you back if restoring a window changed the Space out from under you. Defaults to `true`.",
        "doc": "Whether to send you back if restoring a window changed the Space out from under you. Defaults to `true`.\n\nApplies only after a fully successful restore, where the window is here and being moved was therefore gratuitous. A partial success is left alone: you may well be looking at your window.",
        "name": "returnAfterSpaceChange",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.returnAfterSpaceChange",
        "stripped_doc": [
          "Whether to send you back if restoring a window changed the Space out from under you. Defaults to `true`.",
          "",
          "Applies only after a fully successful restore, where the window is here and being moved was therefore gratuitous. A partial success is left alone: you may well be looking at your window."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.refuseFullscreenSpace",
        "desc": "Whether to refuse to restore anything while the current Space is fullscreen. Defaults to `true`.",
        "doc": "Whether to refuse to restore anything while the current Space is fullscreen. Defaults to `true`.\n\nThe window server will not accept a window into a native fullscreen Space, so every row of the chooser would fail on click. Refusing up front is the honest version of that.",
        "name": "refuseFullscreenSpace",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.refuseFullscreenSpace",
        "stripped_doc": [
          "Whether to refuse to restore anything while the current Space is fullscreen. Defaults to `true`.",
          "",
          "The window server will not accept a window into a native fullscreen Space, so every row of the chooser would fail on click. Refusing up front is the honest version of that."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.includeHidden",
        "desc": "Whether windows of hidden applications (`cmd-H`) are offered alongside minimized ones. Defaults to `false`.",
        "doc": "Whether windows of hidden applications (`cmd-H`) are offered alongside minimized ones. Defaults to `false`.\n\nHiding an application and minimizing a window are different states: a hidden application's windows report `is-hidden` and not `is-minimized`, never appear in `hs.window.minimizedWindows()`, and need `hs.application:unhide()` rather than unminimizing.\nOff by default, which is the answer to \"why is the window I pressed cmd-H on not in the list\".",
        "name": "includeHidden",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.includeHidden",
        "stripped_doc": [
          "Whether windows of hidden applications (`cmd-H`) are offered alongside minimized ones. Defaults to `false`.",
          "",
          "Hiding an application and minimizing a window are different states: a hidden application's windows report `is-hidden` and not `is-minimized`, never appear in `hs.window.minimizedWindows()`, and need `hs.application:unhide()` rather than unminimizing.",
          "Off by default, which is the answer to \"why is the window I pressed cmd-H on not in the list\"."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.includeScratchpad",
        "desc": "Whether yabai scratchpad windows are offered. Defaults to `false`.",
        "doc": "Whether yabai scratchpad windows are offered. Defaults to `false`.\n\nyabai keeps a scratchpad window minimized as its hidden state, so every scratchpad would otherwise appear here, and restoring one behind yabai's back desynchronises its bookkeeping. Use `yabai -m window --toggle <label>` for those.",
        "name": "includeScratchpad",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.includeScratchpad",
        "stripped_doc": [
          "Whether yabai scratchpad windows are offered. Defaults to `false`.",
          "",
          "yabai keeps a scratchpad window minimized as its hidden state, so every scratchpad would otherwise appear here, and restoring one behind yabai's back desynchronises its bookkeeping. Use `yabai -m window --toggle <label>` for those."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.useAccessibilitySweep",
        "desc": "Whether `hs.window.minimizedWindows()` is consulted as well as yabai. Defaults to `true`.",
        "doc": "Whether `hs.window.minimizedWindows()` is consulted as well as yabai. Defaults to `true`.\n\nThe two sources answer different questions: yabai is authoritative about Spaces and window state, while Accessibility is the only one that hands back a real `hs.window`, which is what unminimizing without yabai needs.\nTurning this off makes `DeminimizeWindow:diagnose()` show what yabai alone can do.",
        "name": "useAccessibilitySweep",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.useAccessibilitySweep",
        "stripped_doc": [
          "Whether `hs.window.minimizedWindows()` is consulted as well as yabai. Defaults to `true`.",
          "",
          "The two sources answer different questions: yabai is authoritative about Spaces and window state, while Accessibility is the only one that hands back a real `hs.window`, which is what unminimizing without yabai needs.",
          "Turning this off makes `DeminimizeWindow:diagnose()` show what yabai alone can do."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.showInMenubar",
        "desc": "Whether a menubar item listing minimized windows is created. Defaults to `false`.",
        "doc": "Whether a menubar item listing minimized windows is created. Defaults to `false`.\n\nOff by default: the hotkey is the point, and two sibling Spoons already occupy the menu bar. Takes effect on the next `DeminimizeWindow:start()`.",
        "name": "showInMenubar",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.showInMenubar",
        "stripped_doc": [
          "Whether a menubar item listing minimized windows is created. Defaults to `false`.",
          "",
          "Off by default: the hotkey is the point, and two sibling Spoons already occupy the menu bar. Takes effect on the next `DeminimizeWindow:start()`."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.menubarTitle",
        "desc": "Text shown in the menubar when `DeminimizeWindow.showInMenubar` is on. Defaults to `'▣'`.",
        "doc": "Text shown in the menubar when `DeminimizeWindow.showInMenubar` is on. Defaults to `'▣'`.",
        "name": "menubarTitle",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.menubarTitle",
        "stripped_doc": [
          "Text shown in the menubar when `DeminimizeWindow.showInMenubar` is on. Defaults to `'▣'`."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.chooserRows",
        "desc": "Number of rows visible in the chooser at once. Defaults to `10`.",
        "doc": "Number of rows visible in the chooser at once. Defaults to `10`.",
        "name": "chooserRows",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.chooserRows",
        "stripped_doc": [
          "Number of rows visible in the chooser at once. Defaults to `10`."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.chooserWidth",
        "desc": "Chooser width as a percentage of screen width. Defaults to `35`.",
        "doc": "Chooser width as a percentage of screen width. Defaults to `35`.",
        "name": "chooserWidth",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.chooserWidth",
        "stripped_doc": [
          "Chooser width as a percentage of screen width. Defaults to `35`."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.alertDuration",
        "desc": "Seconds an informational alert stays on screen. Defaults to `1.2`.",
        "doc": "Seconds an informational alert stays on screen. Defaults to `1.2`.",
        "name": "alertDuration",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.alertDuration",
        "stripped_doc": [
          "Seconds an informational alert stays on screen. Defaults to `1.2`."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.titleMax",
        "desc": "Longest window title shown in alerts and diagnostics, in characters. Defaults to `60`.",
        "doc": "Longest window title shown in alerts and diagnostics, in characters. Defaults to `60`.\n\nChooser rows are not truncated: it elides for itself, and a truncated row is one the search field cannot match against.",
        "name": "titleMax",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.titleMax",
        "stripped_doc": [
          "Longest window title shown in alerts and diagnostics, in characters. Defaults to `60`.",
          "",
          "Chooser rows are not truncated: it elides for itself, and a truncated row is one the search field cannot match against."
        ],
        "type": "Variable"
      }
    ],
    "desc": "Brings a minimized window back, onto the Space you are on rather than the one it left.",
    "doc": "Brings a minimized window back, onto the Space you are on rather than the one it left.\n\nClicking a Dock thumbnail restores a window to the Space it was minimized *from*, dragging\nyou to another desktop to go and look at it. This Spoon binds one hotkey to the obvious\nbehaviour instead: with one minimized window it restores it outright, with several it\nopens an `hs.chooser`, with none it says so -- and the window arrives on the Space you are\nalready looking at.\n\nThe hard part is the *placing*, not the unminimizing. `hs.spaces.moveWindowToSpace()` has\ndone nothing since macOS 15 and returns `true` regardless, leaving yabai as the only thing\nthat can move a window between Spaces -- and yabai exits zero for commands it merely\naccepted, so nothing here is believed on the strength of an exit code.\n\nOne rule shapes the Spoon: **place first, reveal second, and never reveal a window you\ncould not place.** A minimized window has no presence on screen, so moving it cannot pull\nthe user anywhere, and once it is verifiably here there is nowhere else to pull them to.\nRevealing first inverts that and fails worse, leaving the window visible somewhere the\nuser cannot see. A ladder of five strategies sits behind the rule, each verified before\nthe next is tried; `DeminimizeWindow:diagnose()` prints what every source can see.",
    "items": [
      {
        "def": "DeminimizeWindow.alertDuration",
        "desc": "Seconds an informational alert stays on screen. Defaults to `1.2`.",
        "doc": "Seconds an informational alert stays on screen. Defaults to `1.2`.",
        "name": "alertDuration",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.alertDuration",
        "stripped_doc": [
          "Seconds an informational alert stays on screen. Defaults to `1.2`."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.allowRevealFirst",
        "desc": "Whether the reveal-first rungs of the ladder may run after placing has failed. Defaults to `true`.",
        "doc": "Whether the reveal-first rungs of the ladder may run after placing has failed. Defaults to `true`.\n\nThe only rungs that can pull you to another Space, and they run only once placing has failed, when the choice is between that risk and not restoring the window at all.\nSet to `false` for a Spoon that is structurally incapable of moving you, and that occasionally does nothing instead.",
        "name": "allowRevealFirst",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.allowRevealFirst",
        "stripped_doc": [
          "Whether the reveal-first rungs of the ladder may run after placing has failed. Defaults to `true`.",
          "",
          "The only rungs that can pull you to another Space, and they run only once placing has failed, when the choice is between that risk and not restoring the window at all.",
          "Set to `false` for a Spoon that is structurally incapable of moving you, and that occasionally does nothing instead."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.chooserRows",
        "desc": "Number of rows visible in the chooser at once. Defaults to `10`.",
        "doc": "Number of rows visible in the chooser at once. Defaults to `10`.",
        "name": "chooserRows",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.chooserRows",
        "stripped_doc": [
          "Number of rows visible in the chooser at once. Defaults to `10`."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.chooserWidth",
        "desc": "Chooser width as a percentage of screen width. Defaults to `35`.",
        "doc": "Chooser width as a percentage of screen width. Defaults to `35`.",
        "name": "chooserWidth",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.chooserWidth",
        "stripped_doc": [
          "Chooser width as a percentage of screen width. Defaults to `35`."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.focusAfterRestore",
        "desc": "Whether the restored window is focused and raised once it has arrived. Defaults to `true`.",
        "doc": "Whether the restored window is focused and raised once it has arrived. Defaults to `true`.\n\nReached only from success: focusing a window that never moved is what drags the user to its Space, so no failure path arrives here.\n`yabai --deminimize` does not focus, so this is always an explicit extra step.",
        "name": "focusAfterRestore",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.focusAfterRestore",
        "stripped_doc": [
          "Whether the restored window is focused and raised once it has arrived. Defaults to `true`.",
          "",
          "Reached only from success: focusing a window that never moved is what drags the user to its Space, so no failure path arrives here.",
          "`yabai --deminimize` does not focus, so this is always an explicit extra step."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.includeHidden",
        "desc": "Whether windows of hidden applications (`cmd-H`) are offered alongside minimized ones. Defaults to `false`.",
        "doc": "Whether windows of hidden applications (`cmd-H`) are offered alongside minimized ones. Defaults to `false`.\n\nHiding an application and minimizing a window are different states: a hidden application's windows report `is-hidden` and not `is-minimized`, never appear in `hs.window.minimizedWindows()`, and need `hs.application:unhide()` rather than unminimizing.\nOff by default, which is the answer to \"why is the window I pressed cmd-H on not in the list\".",
        "name": "includeHidden",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.includeHidden",
        "stripped_doc": [
          "Whether windows of hidden applications (`cmd-H`) are offered alongside minimized ones. Defaults to `false`.",
          "",
          "Hiding an application and minimizing a window are different states: a hidden application's windows report `is-hidden` and not `is-minimized`, never appear in `hs.window.minimizedWindows()`, and need `hs.application:unhide()` rather than unminimizing.",
          "Off by default, which is the answer to \"why is the window I pressed cmd-H on not in the list\"."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.includeScratchpad",
        "desc": "Whether yabai scratchpad windows are offered. Defaults to `false`.",
        "doc": "Whether yabai scratchpad windows are offered. Defaults to `false`.\n\nyabai keeps a scratchpad window minimized as its hidden state, so every scratchpad would otherwise appear here, and restoring one behind yabai's back desynchronises its bookkeeping. Use `yabai -m window --toggle <label>` for those.",
        "name": "includeScratchpad",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.includeScratchpad",
        "stripped_doc": [
          "Whether yabai scratchpad windows are offered. Defaults to `false`.",
          "",
          "yabai keeps a scratchpad window minimized as its hidden state, so every scratchpad would otherwise appear here, and restoring one behind yabai's back desynchronises its bookkeeping. Use `yabai -m window --toggle <label>` for those."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.ladderDeadline",
        "desc": "Seconds after which a restore still in flight is abandoned outright. Defaults to `8`.",
        "doc": "Seconds after which a restore still in flight is abandoned outright. Defaults to `8`.\n\nA backstop for the busy flag rather than a timeout anybody should reach: without it, a restore stranded by an application that never answers would leave the hotkey permanently deaf.",
        "name": "ladderDeadline",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.ladderDeadline",
        "stripped_doc": [
          "Seconds after which a restore still in flight is abandoned outright. Defaults to `8`.",
          "",
          "A backstop for the busy flag rather than a timeout anybody should reach: without it, a restore stranded by an application that never answers would leave the hotkey permanently deaf."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.logger",
        "desc": "Logger object used within the Spoon. Can be accessed to set the default log level for the messages coming from the Spoon.",
        "doc": "Logger object used within the Spoon. Can be accessed to set the default log level for the messages coming from the Spoon.",
        "name": "logger",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.logger",
        "stripped_doc": [
          "Logger object used within the Spoon. Can be accessed to set the default log level for the messages coming from the Spoon."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.menubarTitle",
        "desc": "Text shown in the menubar when `DeminimizeWindow.showInMenubar` is on. Defaults to `'▣'`.",
        "doc": "Text shown in the menubar when `DeminimizeWindow.showInMenubar` is on. Defaults to `'▣'`.",
        "name": "menubarTitle",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.menubarTitle",
        "stripped_doc": [
          "Text shown in the menubar when `DeminimizeWindow.showInMenubar` is on. Defaults to `'▣'`."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.placeTimeout",
        "desc": "Seconds spent confirming that a window really did land on the current Space. Defaults to `0.6`.",
        "doc": "Seconds spent confirming that a window really did land on the current Space. Defaults to `0.6`.\n\nThis is a confirmation window, not a delay. A move that worked is usually visible on the first poll.",
        "name": "placeTimeout",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.placeTimeout",
        "stripped_doc": [
          "Seconds spent confirming that a window really did land on the current Space. Defaults to `0.6`.",
          "",
          "This is a confirmation window, not a delay. A move that worked is usually visible on the first poll."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.pollInterval",
        "desc": "Seconds between checks while confirming a step. Defaults to `0.12`.",
        "doc": "Seconds between checks while confirming a step. Defaults to `0.12`.\n\nEach yabai poll is a subprocess, which is what sets this floor. Checks that can be answered from an `hs.window` run at a quarter of this interval, since they cost nothing but a function call.",
        "name": "pollInterval",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.pollInterval",
        "stripped_doc": [
          "Seconds between checks while confirming a step. Defaults to `0.12`.",
          "",
          "Each yabai poll is a subprocess, which is what sets this floor. Checks that can be answered from an `hs.window` run at a quarter of this interval, since they cost nothing but a function call."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.refuseFullscreenSpace",
        "desc": "Whether to refuse to restore anything while the current Space is fullscreen. Defaults to `true`.",
        "doc": "Whether to refuse to restore anything while the current Space is fullscreen. Defaults to `true`.\n\nThe window server will not accept a window into a native fullscreen Space, so every row of the chooser would fail on click. Refusing up front is the honest version of that.",
        "name": "refuseFullscreenSpace",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.refuseFullscreenSpace",
        "stripped_doc": [
          "Whether to refuse to restore anything while the current Space is fullscreen. Defaults to `true`.",
          "",
          "The window server will not accept a window into a native fullscreen Space, so every row of the chooser would fail on click. Refusing up front is the honest version of that."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.returnAfterSpaceChange",
        "desc": "Whether to send you back if restoring a window changed the Space out from under you. Defaults to `true`.",
        "doc": "Whether to send you back if restoring a window changed the Space out from under you. Defaults to `true`.\n\nApplies only after a fully successful restore, where the window is here and being moved was therefore gratuitous. A partial success is left alone: you may well be looking at your window.",
        "name": "returnAfterSpaceChange",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.returnAfterSpaceChange",
        "stripped_doc": [
          "Whether to send you back if restoring a window changed the Space out from under you. Defaults to `true`.",
          "",
          "Applies only after a fully successful restore, where the window is here and being moved was therefore gratuitous. A partial success is left alone: you may well be looking at your window."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.revealTimeout",
        "desc": "Seconds spent confirming that a window really did come out of the Dock. Defaults to `1.0`.",
        "doc": "Seconds spent confirming that a window really did come out of the Dock. Defaults to `1.0`.\n\nLonger than `DeminimizeWindow.placeTimeout`: the un-minimize animation alone runs about half a second.",
        "name": "revealTimeout",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.revealTimeout",
        "stripped_doc": [
          "Seconds spent confirming that a window really did come out of the Dock. Defaults to `1.0`.",
          "",
          "Longer than `DeminimizeWindow.placeTimeout`: the un-minimize animation alone runs about half a second."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.showInMenubar",
        "desc": "Whether a menubar item listing minimized windows is created. Defaults to `false`.",
        "doc": "Whether a menubar item listing minimized windows is created. Defaults to `false`.\n\nOff by default: the hotkey is the point, and two sibling Spoons already occupy the menu bar. Takes effect on the next `DeminimizeWindow:start()`.",
        "name": "showInMenubar",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.showInMenubar",
        "stripped_doc": [
          "Whether a menubar item listing minimized windows is created. Defaults to `false`.",
          "",
          "Off by default: the hotkey is the point, and two sibling Spoons already occupy the menu bar. Takes effect on the next `DeminimizeWindow:start()`."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.skipChooserForSingle",
        "desc": "Whether a lone minimized window is restored without showing the chooser. Defaults to `true`.",
        "doc": "Whether a lone minimized window is restored without showing the chooser. Defaults to `true`.",
        "name": "skipChooserForSingle",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.skipChooserForSingle",
        "stripped_doc": [
          "Whether a lone minimized window is restored without showing the chooser. Defaults to `true`."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.titleMax",
        "desc": "Longest window title shown in alerts and diagnostics, in characters. Defaults to `60`.",
        "doc": "Longest window title shown in alerts and diagnostics, in characters. Defaults to `60`.\n\nChooser rows are not truncated: it elides for itself, and a truncated row is one the search field cannot match against.",
        "name": "titleMax",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.titleMax",
        "stripped_doc": [
          "Longest window title shown in alerts and diagnostics, in characters. Defaults to `60`.",
          "",
          "Chooser rows are not truncated: it elides for itself, and a truncated row is one the search field cannot match against."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.useAccessibilitySweep",
        "desc": "Whether `hs.window.minimizedWindows()` is consulted as well as yabai. Defaults to `true`.",
        "doc": "Whether `hs.window.minimizedWindows()` is consulted as well as yabai. Defaults to `true`.\n\nThe two sources answer different questions: yabai is authoritative about Spaces and window state, while Accessibility is the only one that hands back a real `hs.window`, which is what unminimizing without yabai needs.\nTurning this off makes `DeminimizeWindow:diagnose()` show what yabai alone can do.",
        "name": "useAccessibilitySweep",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.useAccessibilitySweep",
        "stripped_doc": [
          "Whether `hs.window.minimizedWindows()` is consulted as well as yabai. Defaults to `true`.",
          "",
          "The two sources answer different questions: yabai is authoritative about Spaces and window state, while Accessibility is the only one that hands back a real `hs.window`, which is what unminimizing without yabai needs.",
          "Turning this off makes `DeminimizeWindow:diagnose()` show what yabai alone can do."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.useYabai",
        "desc": "Whether yabai is used to move the restored window onto the current Space. Defaults to `true`.",
        "doc": "Whether yabai is used to move the restored window onto the current Space. Defaults to `true`.\n\nWith this off, or yabai absent, restores go through Accessibility alone, which cannot choose a Space: a window minimized elsewhere reappears there, and the Spoon says so rather than pretending it succeeded.\n`hs.spaces.moveWindowToSpace()` is not an alternative; it has been a silent no-op since macOS 15.",
        "name": "useYabai",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.useYabai",
        "stripped_doc": [
          "Whether yabai is used to move the restored window onto the current Space. Defaults to `true`.",
          "",
          "With this off, or yabai absent, restores go through Accessibility alone, which cannot choose a Space: a window minimized elsewhere reappears there, and the Spoon says so rather than pretending it succeeded.",
          "`hs.spaces.moveWindowToSpace()` is not an alternative; it has been a silent no-op since macOS 15."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.yabaiPath",
        "desc": "Absolute path to the yabai binary, or `nil` to probe the usual locations. Defaults to `nil`.",
        "doc": "Absolute path to the yabai binary, or `nil` to probe the usual locations. Defaults to `nil`.\n\nOnly needed for an installation somewhere unusual. Homebrew on both architectures, nix-darwin and `~/.local/bin` are probed already.",
        "name": "yabaiPath",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.yabaiPath",
        "stripped_doc": [
          "Absolute path to the yabai binary, or `nil` to probe the usual locations. Defaults to `nil`.",
          "",
          "Only needed for an installation somewhere unusual. Homebrew on both architectures, nix-darwin and `~/.local/bin` are probed already."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow.yabaiTimeout",
        "desc": "Seconds any single yabai command may take before it is abandoned. Defaults to `1.5`.",
        "doc": "Seconds any single yabai command may take before it is abandoned. Defaults to `1.5`.",
        "name": "yabaiTimeout",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "DeminimizeWindow.yabaiTimeout",
        "stripped_doc": [
          "Seconds any single yabai command may take before it is abandoned. Defaults to `1.5`."
        ],
        "type": "Variable"
      },
      {
        "def": "DeminimizeWindow:abortYabai() -> self",
        "desc": "Kills any yabai commands in flight and abandons whatever they were part of.",
        "doc": "Kills any yabai commands in flight and abandons whatever they were part of.\n\nParameters:\n * None\n\nReturns:\n * The DeminimizeWindow object\n\nCalled from `DeminimizeWindow:stop()`. Each job is marked settled *before* its process is signalled: terminating a task causes a completion callback rather than cancelling one, so without the flag a config reload would answer into a half-torn-down Spoon and walk the ladder on into a `--focus`.",
        "name": "abortYabai",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The DeminimizeWindow object",
          "",
          "Called from `DeminimizeWindow:stop()`. Each job is marked settled *before* its process is signalled: terminating a task causes a completion callback rather than cancelling one, so without the flag a config reload would answer into a half-torn-down Spoon and walk the ladder on into a `--focus`."
        ],
        "signature": "DeminimizeWindow:abortYabai() -> self",
        "stripped_doc": [
          "Kills any yabai commands in flight and abandons whatever they were part of.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "DeminimizeWindow:bindHotkeys(mapping) -> self",
        "desc": "Binds hotkeys for DeminimizeWindow.",
        "doc": "Binds hotkeys for DeminimizeWindow.\n\nParameters:\n * mapping - A table containing hotkey modifier/key details for the following items:\n   * restore - Restore a minimized window, showing the chooser only when there is a choice\n   * show - Always show the chooser, whatever the number of minimized windows\n\nReturns:\n * The DeminimizeWindow object\n\nFor example: `spoon.DeminimizeWindow:bindHotkeys({ restore = { { \"cmd\", \"alt\", \"ctrl\" }, \"M\" } })`",
        "name": "bindHotkeys",
        "notes": [],
        "parameters": [
          " * mapping - A table containing hotkey modifier/key details for the following items:",
          "   * restore - Restore a minimized window, showing the chooser only when there is a choice",
          "   * show - Always show the chooser, whatever the number of minimized windows",
          ""
        ],
        "returns": [
          " * The DeminimizeWindow object",
          "",
          "For example: `spoon.DeminimizeWindow:bindHotkeys({ restore = { { \"cmd\", \"alt\", \"ctrl\" }, \"M\" } })`"
        ],
        "signature": "DeminimizeWindow:bindHotkeys(mapping) -> self",
        "stripped_doc": [
          "Binds hotkeys for DeminimizeWindow.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "DeminimizeWindow:collect(done) -> self",
        "desc": "Builds the list of restorable windows and hands it to a callback.",
        "doc": "Builds the list of restorable windows and hands it to a callback.\n\nParameters:\n * done - A function called as `done(items, space, err)`. `items` is a sorted list of tables with `winId`, `appName`, `title` and `minimized` keys; `space` describes the Space they would be restored to; both are nil when `err` is set\n\nReturns:\n * The DeminimizeWindow object\n\nUnions two sources by window id, the same `CGWindowID` in both, so the join needs no guessing. yabai is authoritative about window state and Spaces; Accessibility contributes the `hs.window` the yabai-free rungs need.\nEither source alone yields a usable entry: a window only yabai can see is restorable by yabai only, and one only Accessibility can see cannot be placed with confidence.",
        "name": "collect",
        "notes": [],
        "parameters": [
          " * done - A function called as `done(items, space, err)`. `items` is a sorted list of tables with `winId`, `appName`, `title` and `minimized` keys; `space` describes the Space they would be restored to; both are nil when `err` is set",
          ""
        ],
        "returns": [
          " * The DeminimizeWindow object",
          "",
          "Unions two sources by window id, the same `CGWindowID` in both, so the join needs no guessing. yabai is authoritative about window state and Spaces; Accessibility contributes the `hs.window` the yabai-free rungs need.",
          "Either source alone yields a usable entry: a window only yabai can see is restorable by yabai only, and one only Accessibility can see cannot be placed with confidence."
        ],
        "signature": "DeminimizeWindow:collect(done) -> self",
        "stripped_doc": [
          "Builds the list of restorable windows and hands it to a callback.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "DeminimizeWindow:diagnose([done]) -> self",
        "desc": "Prints what each source can see, and why a window is or is not being offered.",
        "doc": "Prints what each source can see, and why a window is or is not being offered.\n\nParameters:\n * done - An optional function called with the report text once it has been gathered\n\nReturns:\n * The DeminimizeWindow object\n\nAsynchronous, because it asks yabai. The report is printed to the Console when it arrives, so calling this bare from the Console is the normal way to use it.\nThe decisive line is the per-window one: a row tagged only `yabai` has no `hs.window`, so the Accessibility rungs do not apply to it, and a row tagged only `ax` cannot be placed with confidence.",
        "name": "diagnose",
        "notes": [],
        "parameters": [
          " * done - An optional function called with the report text once it has been gathered",
          ""
        ],
        "returns": [
          " * The DeminimizeWindow object",
          "",
          "Asynchronous, because it asks yabai. The report is printed to the Console when it arrives, so calling this bare from the Console is the normal way to use it.",
          "The decisive line is the per-window one: a row tagged only `yabai` has no `hs.window`, so the Accessibility rungs do not apply to it, and a row tagged only `ax` cannot be placed with confidence."
        ],
        "signature": "DeminimizeWindow:diagnose([done]) -> self",
        "stripped_doc": [
          "Prints what each source can see, and why a window is or is not being offered.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "DeminimizeWindow:hide() -> self",
        "desc": "Closes the chooser if it is open.",
        "doc": "Closes the chooser if it is open.\n\nParameters:\n * None\n\nReturns:\n * The DeminimizeWindow object",
        "name": "hide",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The DeminimizeWindow object"
        ],
        "signature": "DeminimizeWindow:hide() -> self",
        "stripped_doc": [
          "Closes the chooser if it is open.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "DeminimizeWindow:init() -> self",
        "desc": "Prepares the Spoon. Called automatically by `hs.loadSpoon()`.",
        "doc": "Prepares the Spoon. Called automatically by `hs.loadSpoon()`.\n\nParameters:\n * None\n\nReturns:\n * The DeminimizeWindow object\n\nDeliberately starts nothing. The chooser and the menubar item both belong to `DeminimizeWindow:start()`.\nDeliberately empty rather than re-initialising state: `hs.loadSpoon()` reaches `init()` through `require()`, which returns a cached object on a second load, so clearing state here would strand a live chooser.",
        "name": "init",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The DeminimizeWindow object",
          "",
          "Deliberately starts nothing. The chooser and the menubar item both belong to `DeminimizeWindow:start()`.",
          "Deliberately empty rather than re-initialising state: `hs.loadSpoon()` reaches `init()` through `require()`, which returns a cached object on a second load, so clearing state here would strand a live chooser."
        ],
        "signature": "DeminimizeWindow:init() -> self",
        "stripped_doc": [
          "Prepares the Spoon. Called automatically by `hs.loadSpoon()`.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "DeminimizeWindow:restore() -> self",
        "desc": "Restores a minimized window, asking which one only when there is a choice.",
        "doc": "Restores a minimized window, asking which one only when there is a choice.\n\nParameters:\n * None\n\nReturns:\n * The DeminimizeWindow object\n\nWith no minimized windows this shows a brief alert, with exactly one it restores it outright, with more it opens the chooser. See `DeminimizeWindow.skipChooserForSingle`.\nThis is the method `DeminimizeWindow:bindHotkeys()` binds; pressing the hotkey while the chooser is open closes it again.",
        "name": "restore",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The DeminimizeWindow object",
          "",
          "With no minimized windows this shows a brief alert, with exactly one it restores it outright, with more it opens the chooser. See `DeminimizeWindow.skipChooserForSingle`.",
          "This is the method `DeminimizeWindow:bindHotkeys()` binds; pressing the hotkey while the chooser is open closes it again."
        ],
        "signature": "DeminimizeWindow:restore() -> self",
        "stripped_doc": [
          "Restores a minimized window, asking which one only when there is a choice.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "DeminimizeWindow:restoreById(winId) -> self",
        "desc": "Restores one minimized window by its window id, onto the current Space.",
        "doc": "Restores one minimized window by its window id, onto the current Space.\n\nParameters:\n * winId - The window id, as returned in the `winId` field of `DeminimizeWindow:collect()`\n\nReturns:\n * The DeminimizeWindow object\n\nThe id must have come from a `DeminimizeWindow:collect()` pass, since everything else known about the window is looked up in the map that call builds.\nThe target Space and the window's state are re-read here rather than taken from the list, because a chooser can sit open across a Space change.",
        "name": "restoreById",
        "notes": [],
        "parameters": [
          " * winId - The window id, as returned in the `winId` field of `DeminimizeWindow:collect()`",
          ""
        ],
        "returns": [
          " * The DeminimizeWindow object",
          "",
          "The id must have come from a `DeminimizeWindow:collect()` pass, since everything else known about the window is looked up in the map that call builds.",
          "The target Space and the window's state are re-read here rather than taken from the list, because a chooser can sit open across a Space change."
        ],
        "signature": "DeminimizeWindow:restoreById(winId) -> self",
        "stripped_doc": [
          "Restores one minimized window by its window id, onto the current Space.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "DeminimizeWindow:show() -> self",
        "desc": "Opens the chooser, whatever the number of minimized windows.",
        "doc": "Opens the chooser, whatever the number of minimized windows.\n\nParameters:\n * None\n\nReturns:\n * The DeminimizeWindow object\n\nUnlike `DeminimizeWindow:restore()` this never restores anything by itself, so it is the method to bind if you would always rather see the list first.",
        "name": "show",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The DeminimizeWindow object",
          "",
          "Unlike `DeminimizeWindow:restore()` this never restores anything by itself, so it is the method to bind if you would always rather see the list first."
        ],
        "signature": "DeminimizeWindow:show() -> self",
        "stripped_doc": [
          "Opens the chooser, whatever the number of minimized windows.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "DeminimizeWindow:start() -> self",
        "desc": "Makes the Spoon ready to restore windows, and adds the menubar item if it is enabled.",
        "doc": "Makes the Spoon ready to restore windows, and adds the menubar item if it is enabled.\n\nParameters:\n * None\n\nReturns:\n * The DeminimizeWindow object\n\nCalling this on an already-started Spoon restarts it cleanly.\nNothing polls and no timer runs while idle. The window list is built on demand, when the hotkey is pressed or the menu is pulled down.\nWarns via `hs.alert` if Accessibility permission has not been granted, since nothing works without it.",
        "name": "start",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The DeminimizeWindow object",
          "",
          "Calling this on an already-started Spoon restarts it cleanly.",
          "Nothing polls and no timer runs while idle. The window list is built on demand, when the hotkey is pressed or the menu is pulled down.",
          "Warns via `hs.alert` if Accessibility permission has not been granted, since nothing works without it."
        ],
        "signature": "DeminimizeWindow:start() -> self",
        "stripped_doc": [
          "Makes the Spoon ready to restore windows, and adds the menubar item if it is enabled.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "DeminimizeWindow:status() -> table",
        "desc": "Returns the Spoon's current state, for poking at from the Hammerspoon Console.",
        "doc": "Returns the Spoon's current state, for poking at from the Hammerspoon Console.\n\nParameters:\n * None\n\nReturns:\n * A table with `running`, `menubar`, `chooserVisible`, `restoring`, `yabai`, `yabaiBusy`, `yabaiLastError`, `listed` and `timers` keys\n\n`yabai` is the resolved binary path, or `false` when yabai is off or not installed. It says nothing about whether the yabai *service* is running; only `DeminimizeWindow:diagnose()` answers that.\n`listed` counts the windows in the most recent list, which may be stale; the list is never reused across a press.",
        "name": "status",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * A table with `running`, `menubar`, `chooserVisible`, `restoring`, `yabai`, `yabaiBusy`, `yabaiLastError`, `listed` and `timers` keys",
          "",
          "`yabai` is the resolved binary path, or `false` when yabai is off or not installed. It says nothing about whether the yabai *service* is running; only `DeminimizeWindow:diagnose()` answers that.",
          "`listed` counts the windows in the most recent list, which may be stale; the list is never reused across a press."
        ],
        "signature": "DeminimizeWindow:status() -> table",
        "stripped_doc": [
          "Returns the Spoon's current state, for poking at from the Hammerspoon Console.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "DeminimizeWindow:stop() -> self",
        "desc": "Abandons any restore in flight, destroys the chooser and removes the menubar item.",
        "doc": "Abandons any restore in flight, destroys the chooser and removes the menubar item.\n\nParameters:\n * None\n\nReturns:\n * The DeminimizeWindow object\n\nAny hotkeys bound with `DeminimizeWindow:bindHotkeys()` stay bound; rebind or delete them separately if you want them gone.",
        "name": "stop",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The DeminimizeWindow object",
          "",
          "Any hotkeys bound with `DeminimizeWindow:bindHotkeys()` stay bound; rebind or delete them separately if you want them gone."
        ],
        "signature": "DeminimizeWindow:stop() -> self",
        "stripped_doc": [
          "Abandons any restore in flight, destroys the chooser and removes the menubar item.",
          ""
        ],
        "type": "Method"
      }
    ],
    "name": "DeminimizeWindow",
    "stripped_doc": [],
    "submodules": [],
    "type": "Module"
  }
]
