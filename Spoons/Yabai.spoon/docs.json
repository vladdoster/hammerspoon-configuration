[
  {
    "Command": [],
    "Constant": [],
    "Constructor": [],
    "Deprecated": [],
    "Field": [],
    "Function": [],
    "Method": [
      {
        "def": "Yabai:listSpaces(done) -> self",
        "desc": "Builds the current Space model and hands it to a callback.",
        "doc": "Builds the current Space model and hands it to a callback.\n\nParameters:\n * done - Called as `done(entries, err)`. `entries` is an array of tables carrying `id`, `index`, `label`, `screenName`, `displayIndex`, `managedCount`, `isActive`, `isFullscreen` and `blockedReason`, or nil and a message if the world could not be read\n\nReturns:\n * The Yabai object\n\nAsynchronous because the yabai path is: three socket queries, none of which may block the runloop. Exposed for prodding from the Console, where `hs.inspect` on the result is the fastest way to see what the chooser is about to show.",
        "name": "listSpaces",
        "notes": [],
        "parameters": [
          " * done - Called as `done(entries, err)`. `entries` is an array of tables carrying `id`, `index`, `label`, `screenName`, `displayIndex`, `managedCount`, `isActive`, `isFullscreen` and `blockedReason`, or nil and a message if the world could not be read",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "Asynchronous because the yabai path is: three socket queries, none of which may block the runloop. Exposed for prodding from the Console, where `hs.inspect` on the result is the fastest way to see what the chooser is about to show."
        ],
        "signature": "Yabai:listSpaces(done) -> self",
        "stripped_doc": [
          "Builds the current Space model and hands it to a callback.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:listDisplays(done) -> self",
        "desc": "Lists the attached displays, with the number of Spaces on each.",
        "doc": "Lists the attached displays, with the number of Spaces on each.\n\nParameters:\n * done - Called as `done(entries, err)`. `entries` is an array of tables carrying `index`, `name`, `spaceCount`, `isActive` and `screen`\n\nReturns:\n * The Yabai object\n\n`index` is yabai's display number, which is what every yabai display selector wants, and `screen` is the matching `hs.screen` for the fallbacks, which speak in screen objects instead.",
        "name": "listDisplays",
        "notes": [],
        "parameters": [
          " * done - Called as `done(entries, err)`. `entries` is an array of tables carrying `index`, `name`, `spaceCount`, `isActive` and `screen`",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "`index` is yabai's display number, which is what every yabai display selector wants, and `screen` is the matching `hs.screen` for the fallbacks, which speak in screen objects instead."
        ],
        "signature": "Yabai:listDisplays(done) -> self",
        "stripped_doc": [
          "Lists the attached displays, with the number of Spaces on each.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:listWindows(done) -> self",
        "desc": "Lists every window that is not our own, with the Space it is on.",
        "doc": "Lists every window that is not our own, with the Space it is on.\n\nParameters:\n * done - Called as `done(entries, err)`. `entries` is an array of tables carrying `winId`, `appName`, `title`, `spaceId`, `spaceIndex`, `spaceName`, `displayIndex` and `minimized`\n\nReturns:\n * The Yabai object\n\nOwn windows are excluded by pid rather than by application name. `hs.window:application()` logs from Hammerspoon's C layer and returns nil transiently even for a live process, so a guard written on it fails open; the pid a window already carries cannot.\n\nMinimized windows are left out unless `Yabai.showMinimized` is set. They have no Space in any useful sense, and `DeminimizeWindow` is the Spoon that deals with them.",
        "name": "listWindows",
        "notes": [],
        "parameters": [
          " * done - Called as `done(entries, err)`. `entries` is an array of tables carrying `winId`, `appName`, `title`, `spaceId`, `spaceIndex`, `spaceName`, `displayIndex` and `minimized`",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "Own windows are excluded by pid rather than by application name. `hs.window:application()` logs from Hammerspoon's C layer and returns nil transiently even for a live process, so a guard written on it fails open; the pid a window already carries cannot.",
          "",
          "Minimized windows are left out unless `Yabai.showMinimized` is set. They have no Space in any useful sense, and `DeminimizeWindow` is the Spoon that deals with them."
        ],
        "signature": "Yabai:listWindows(done) -> self",
        "stripped_doc": [
          "Lists every window that is not our own, with the Space it is on.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:focusSpaceById(id) -> self",
        "desc": "Switches to one Space, named by its stable Space id.",
        "doc": "Switches to one Space, named by its stable Space id.\n\nParameters:\n * id - The Space id, as reported by `Yabai:listSpaces()` or `hs.spaces`\n\nReturns:\n * The Yabai object\n\nThe one verb that needs nothing special from yabai: `space --focus` goes over the plain socket, so this works on a machine with System Integrity Protection fully enabled, and `hs.spaces.gotoSpace` stands behind it regardless.",
        "name": "focusSpaceById",
        "notes": [],
        "parameters": [
          " * id - The Space id, as reported by `Yabai:listSpaces()` or `hs.spaces`",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "The one verb that needs nothing special from yabai: `space --focus` goes over the plain socket, so this works on a machine with System Integrity Protection fully enabled, and `hs.spaces.gotoSpace` stands behind it regardless."
        ],
        "signature": "Yabai:focusSpaceById(id) -> self",
        "stripped_doc": [
          "Switches to one Space, named by its stable Space id.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:createSpaceOnDisplay(displayIndex) -> self",
        "desc": "Adds a Space to the end of one display.",
        "doc": "Adds a Space to the end of one display.\n\nParameters:\n * displayIndex - The yabai display number, as reported by `Yabai:listDisplays()`\n\nReturns:\n * The Yabai object\n\nmacOS always appends: neither backend can insert a Space at a chosen position, so the new Space lands last and `Yabai:moveSpaceToPosition()` is what puts it somewhere else.",
        "name": "createSpaceOnDisplay",
        "notes": [],
        "parameters": [
          " * displayIndex - The yabai display number, as reported by `Yabai:listDisplays()`",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "macOS always appends: neither backend can insert a Space at a chosen position, so the new Space lands last and `Yabai:moveSpaceToPosition()` is what puts it somewhere else."
        ],
        "signature": "Yabai:createSpaceOnDisplay(displayIndex) -> self",
        "stripped_doc": [
          "Adds a Space to the end of one display.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:deleteById(id) -> self",
        "desc": "Deletes one Space, named by its stable Space id.",
        "doc": "Deletes one Space, named by its stable Space id.\n\nParameters:\n * id - The Space id, as reported by `Yabai:listSpaces()` or `hs.spaces`\n\nReturns:\n * The Yabai object\n\nRe-reads every Space before acting. The id the chooser handed back is stable, but the Mission Control position both backends select on is not, and the list may be seconds old by the time a choice is made, so the position is resolved from the id here and the Space is re-checked for having become active or last-on-its-screen in the meantime.\n\nOne Space per invocation, deliberately. Those positions renumber the moment a Space goes away, so a batch loop is a loop whose target drifts under it.",
        "name": "deleteById",
        "notes": [],
        "parameters": [
          " * id - The Space id, as reported by `Yabai:listSpaces()` or `hs.spaces`",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "Re-reads every Space before acting. The id the chooser handed back is stable, but the Mission Control position both backends select on is not, and the list may be seconds old by the time a choice is made, so the position is resolved from the id here and the Space is re-checked for having become active or last-on-its-screen in the meantime.",
          "",
          "One Space per invocation, deliberately. Those positions renumber the moment a Space goes away, so a batch loop is a loop whose target drifts under it."
        ],
        "signature": "Yabai:deleteById(id) -> self",
        "stripped_doc": [
          "Deletes one Space, named by its stable Space id.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:moveSpaceToPosition(id, position) -> self",
        "desc": "Moves a Space to another Mission Control position on the display it is already on.",
        "doc": "Moves a Space to another Mission Control position on the display it is already on.\n\nParameters:\n * id - The Space id to move\n * position - The Mission Control position to move it to, counted across all displays as yabai counts them\n\nReturns:\n * The Yabai object\n\nyabai only. `hs.spaces` can create, remove and switch Spaces but has no way to reorder them, so where yabai cannot do this nothing can, and the failure says so rather than pretending a fallback was tried.",
        "name": "moveSpaceToPosition",
        "notes": [],
        "parameters": [
          " * id - The Space id to move",
          " * position - The Mission Control position to move it to, counted across all displays as yabai counts them",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "yabai only. `hs.spaces` can create, remove and switch Spaces but has no way to reorder them, so where yabai cannot do this nothing can, and the failure says so rather than pretending a fallback was tried."
        ],
        "signature": "Yabai:moveSpaceToPosition(id, position) -> self",
        "stripped_doc": [
          "Moves a Space to another Mission Control position on the display it is already on.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:moveSpaceToDisplay(id, displayIndex) -> self",
        "desc": "Sends a whole Space, and everything on it, to another display.",
        "doc": "Sends a whole Space, and everything on it, to another display.\n\nParameters:\n * id - The Space id to send\n * displayIndex - The yabai display number to send it to\n\nReturns:\n * The Yabai object\n\nyabai only, for the same reason as `Yabai:moveSpaceToPosition()`: `hs.spaces` cannot move a Space between displays at all.",
        "name": "moveSpaceToDisplay",
        "notes": [],
        "parameters": [
          " * id - The Space id to send",
          " * displayIndex - The yabai display number to send it to",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "yabai only, for the same reason as `Yabai:moveSpaceToPosition()`: `hs.spaces` cannot move a Space between displays at all."
        ],
        "signature": "Yabai:moveSpaceToDisplay(id, displayIndex) -> self",
        "stripped_doc": [
          "Sends a whole Space, and everything on it, to another display.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:focusWindowById(winId) -> self",
        "desc": "Focuses one window, following it onto whatever Space it is on.",
        "doc": "Focuses one window, following it onto whatever Space it is on.\n\nParameters:\n * winId - The window id, as reported by `Yabai:listWindows()` or `hs.window:id()`\n\nReturns:\n * The Yabai object\n\nNeeds nothing from yabai's scripting addition, and `hs.window:focus()` stands behind it, so this is the one window verb that works everywhere.",
        "name": "focusWindowById",
        "notes": [],
        "parameters": [
          " * winId - The window id, as reported by `Yabai:listWindows()` or `hs.window:id()`",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "Needs nothing from yabai's scripting addition, and `hs.window:focus()` stands behind it, so this is the one window verb that works everywhere."
        ],
        "signature": "Yabai:focusWindowById(winId) -> self",
        "stripped_doc": [
          "Focuses one window, following it onto whatever Space it is on.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:moveWindowToSpace(winId, spaceId) -> self",
        "desc": "Moves one window to another Space.",
        "doc": "Moves one window to another Space.\n\nParameters:\n * winId - The window id to move\n * spaceId - The stable Space id to move it to\n\nReturns:\n * The Yabai object\n\nyabai is the real path here. `hs.spaces.moveWindowToSpace()` is tried behind it and reported honestly, but it has been a silent no-op since macOS 15 -- it returns success and moves nothing -- which is why the result is polled rather than believed. `SummonWindow` carries a third rung that borrows the mouse pointer to drag the window across in Mission Control; it is deliberately not duplicated here.",
        "name": "moveWindowToSpace",
        "notes": [],
        "parameters": [
          " * winId - The window id to move",
          " * spaceId - The stable Space id to move it to",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "yabai is the real path here. `hs.spaces.moveWindowToSpace()` is tried behind it and reported honestly, but it has been a silent no-op since macOS 15 -- it returns success and moves nothing -- which is why the result is polled rather than believed. `SummonWindow` carries a third rung that borrows the mouse pointer to drag the window across in Mission Control; it is deliberately not duplicated here."
        ],
        "signature": "Yabai:moveWindowToSpace(winId, spaceId) -> self",
        "stripped_doc": [
          "Moves one window to another Space.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:moveWindowToDisplay(winId, displayIndex) -> self",
        "desc": "Sends one window to another display.",
        "doc": "Sends one window to another display.\n\nParameters:\n * winId - The window id to send\n * displayIndex - The yabai display number to send it to\n\nReturns:\n * The Yabai object\n\n`hs.window:moveToScreen()` stands behind yabai here and genuinely works, because moving a window between displays does not cross a Space and so needs no scripting addition.",
        "name": "moveWindowToDisplay",
        "notes": [],
        "parameters": [
          " * winId - The window id to send",
          " * displayIndex - The yabai display number to send it to",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "`hs.window:moveToScreen()` stands behind yabai here and genuinely works, because moving a window between displays does not cross a Space and so needs no scripting addition."
        ],
        "signature": "Yabai:moveWindowToDisplay(winId, displayIndex) -> self",
        "stripped_doc": [
          "Sends one window to another display.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:choicesFor(entries) -> table",
        "desc": "Renders a Space model as chooser rows for the delete screen, blocked Spaces included.",
        "doc": "Renders a Space model as chooser rows for the delete screen, blocked Spaces included.\n\nParameters:\n * entries - A Space model, as `Yabai:listSpaces()` produces\n\nReturns:\n * A list of `hs.chooser` choice tables\n\nA Space macOS refuses to remove is rendered with its reason and made unselectable rather than dropped, so the list still matches what Mission Control shows.",
        "name": "choicesFor",
        "notes": [],
        "parameters": [
          " * entries - A Space model, as `Yabai:listSpaces()` produces",
          ""
        ],
        "returns": [
          " * A list of `hs.chooser` choice tables",
          "",
          "A Space macOS refuses to remove is rendered with its reason and made unselectable rather than dropped, so the list still matches what Mission Control shows."
        ],
        "signature": "Yabai:choicesFor(entries) -> table",
        "stripped_doc": [
          "Renders a Space model as chooser rows for the delete screen, blocked Spaces included.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:back() -> self",
        "desc": "Steps one level back, dropping the pick made on the level being left.",
        "doc": "Steps one level back, dropping the pick made on the level being left.\n\nParameters:\n * None\n\nReturns:\n * The Yabai object\n\nWhat Escape, Delete and the `← Back` row all call, in either front end. On the first operand level it returns to the verb list; at the verb list there is nothing left to pop, so the chooser dismisses and the modal leaves.",
        "name": "back",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "What Escape, Delete and the `← Back` row all call, in either front end. On the first operand level it returns to the verb list; at the verb list there is nothing left to pop, so the chooser dismisses and the modal leaves."
        ],
        "signature": "Yabai:back() -> self",
        "stripped_doc": [
          "Steps one level back, dropping the pick made on the level being left.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:show() -> self",
        "desc": "Opens the chooser at the verb list.",
        "doc": "Opens the chooser at the verb list.\n\nParameters:\n * None\n\nReturns:\n * The Yabai object\n\nWhat the menubar item's click opens, and what `Yabai:searchHere()` falls back to. Any half-finished flow is abandoned, so this always starts from the top; `/` inside the modal is the way to open the chooser mid-verb instead.",
        "name": "show",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "What the menubar item's click opens, and what `Yabai:searchHere()` falls back to. Any half-finished flow is abandoned, so this always starts from the top; `/` inside the modal is the way to open the chooser mid-verb instead."
        ],
        "signature": "Yabai:show() -> self",
        "stripped_doc": [
          "Opens the chooser at the verb list.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:hide() -> self",
        "desc": "Closes the chooser, abandoning any half-finished flow.",
        "doc": "Closes the chooser, abandoning any half-finished flow.\n\nParameters:\n * None\n\nReturns:\n * The Yabai object",
        "name": "hide",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The Yabai object"
        ],
        "signature": "Yabai:hide() -> self",
        "stripped_doc": [
          "Closes the chooser, abandoning any half-finished flow.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:assignKeys(rows) -> table",
        "desc": "Gives every selectable row a key and returns the key-to-row map the modal dispatches through.",
        "doc": "Gives every selectable row a key and returns the key-to-row map the modal dispatches through.\n\nParameters:\n * rows - A list of choice tables, as the operand builders and `Yabai:verbChoices()` produce\n\nReturns:\n * A table mapping each assigned key to its row\n\nA row's own `hint` wins where it is still free -- the Space index, the display number, the verb's mnemonic -- and everything else takes the next unused letter from `Yabai.hintKeys`.\n\nA row that cannot be chosen is left keyless rather than skipped over, so a Space in the middle of the list that macOS refuses to delete does not shift the digits of the ones after it. The keys on the panel keep matching the numbers in Mission Control.",
        "name": "assignKeys",
        "notes": [],
        "parameters": [
          " * rows - A list of choice tables, as the operand builders and `Yabai:verbChoices()` produce",
          ""
        ],
        "returns": [
          " * A table mapping each assigned key to its row",
          "",
          "A row's own `hint` wins where it is still free -- the Space index, the display number, the verb's mnemonic -- and everything else takes the next unused letter from `Yabai.hintKeys`.",
          "",
          "A row that cannot be chosen is left keyless rather than skipped over, so a Space in the middle of the list that macOS refuses to delete does not shift the digits of the ones after it. The keys on the panel keep matching the numbers in Mission Control."
        ],
        "signature": "Yabai:assignKeys(rows) -> table",
        "stripped_doc": [
          "Gives every selectable row a key and returns the key-to-row map the modal dispatches through.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:showLevel() -> self",
        "desc": "Rebuilds the panel for wherever the flow currently stands: the verb list, or one operand level.",
        "doc": "Rebuilds the panel for wherever the flow currently stands: the verb list, or one operand level.\n\nParameters:\n * None\n\nReturns:\n * The Yabai object\n\nThe modal's half of `Yabai:showStep()`, and the direct counterpart of `Yabai:showOperand()`. Both read the same flow and the same operand builders; only the rendering differs.",
        "name": "showLevel",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "The modal's half of `Yabai:showStep()`, and the direct counterpart of `Yabai:showOperand()`. Both read the same flow and the same operand builders; only the rendering differs."
        ],
        "signature": "Yabai:showLevel() -> self",
        "stripped_doc": [
          "Rebuilds the panel for wherever the flow currently stands: the verb list, or one operand level.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:onKey(char) -> self",
        "desc": "Routes one keystroke against the level currently on the panel.",
        "doc": "Routes one keystroke against the level currently on the panel.\n\nParameters:\n * char - The single character the modal was handed\n\nReturns:\n * The Yabai object\n\nEvery letter and digit reaches this, bound or not. One with nothing behind it is swallowed and reported in the panel's footer, because a modal that lets a stray keystroke through into the window underneath is worse than one that says no.",
        "name": "onKey",
        "notes": [],
        "parameters": [
          " * char - The single character the modal was handed",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "Every letter and digit reaches this, bound or not. One with nothing behind it is swallowed and reported in the panel's footer, because a modal that lets a stray keystroke through into the window underneath is worse than one that says no."
        ],
        "signature": "Yabai:onKey(char) -> self",
        "stripped_doc": [
          "Routes one keystroke against the level currently on the panel.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:searchHere() -> self",
        "desc": "Leaves the modal and reopens the same half-finished operation in the chooser.",
        "doc": "Leaves the modal and reopens the same half-finished operation in the chooser.\n\nParameters:\n * None\n\nReturns:\n * The Yabai object\n\nWhat `/` does. The two front ends push and pop the same flow, so the chooser opens on the level the modal was standing on with every pick so far intact, and a window that is easier to find by typing is found by typing.",
        "name": "searchHere",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "What `/` does. The two front ends push and pop the same flow, so the chooser opens on the level the modal was standing on with every pick so far intact, and a window that is easier to find by typing is found by typing."
        ],
        "signature": "Yabai:searchHere() -> self",
        "stripped_doc": [
          "Leaves the modal and reopens the same half-finished operation in the chooser.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:enterModal() -> self",
        "desc": "Enters the modal at the verb list.",
        "doc": "Enters the modal at the verb list.\n\nParameters:\n * None\n\nReturns:\n * The Yabai object\n\nWhat the leader hotkey is bound to. Any half-finished flow is abandoned, so this always starts from the verb list, and every letter and digit belongs to the modal until it is left: by finishing a verb, by Escape at the verb list, or by `Yabai.modalTimeout` seconds of doing nothing.",
        "name": "enterModal",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "What the leader hotkey is bound to. Any half-finished flow is abandoned, so this always starts from the verb list, and every letter and digit belongs to the modal until it is left: by finishing a verb, by Escape at the verb list, or by `Yabai.modalTimeout` seconds of doing nothing."
        ],
        "signature": "Yabai:enterModal() -> self",
        "stripped_doc": [
          "Enters the modal at the verb list.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:exitModal() -> self",
        "desc": "Leaves the modal and takes the panel down.",
        "doc": "Leaves the modal and takes the panel down.\n\nParameters:\n * None\n\nReturns:\n * The Yabai object\n\nAny half-finished flow is left alone rather than dropped, which is what lets `Yabai:searchHere()` hand it to the chooser mid-verb.",
        "name": "exitModal",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "Any half-finished flow is left alone rather than dropped, which is what lets `Yabai:searchHere()` hand it to the chooser mid-verb."
        ],
        "signature": "Yabai:exitModal() -> self",
        "stripped_doc": [
          "Leaves the modal and takes the panel down.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:keymap() -> table",
        "desc": "Reports the keys the level currently on the panel answers to.",
        "doc": "Reports the keys the level currently on the panel answers to.\n\nParameters:\n * None\n\nReturns:\n * A table mapping each live key to the row it would choose, empty outside the modal\n\nFor prodding from the Console: `hs.inspect(spoon.Yabai:keymap())` is the quickest way to see what a level assigned without reading the panel off the screen.",
        "name": "keymap",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * A table mapping each live key to the row it would choose, empty outside the modal",
          "",
          "For prodding from the Console: `hs.inspect(spoon.Yabai:keymap())` is the quickest way to see what a level assigned without reading the panel off the screen."
        ],
        "signature": "Yabai:keymap() -> table",
        "stripped_doc": [
          "Reports the keys the level currently on the panel answers to.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:start() -> self",
        "desc": "Builds the modal, creates the menubar item and starts watching for Space changes.",
        "doc": "Builds the modal, creates the menubar item and starts watching for Space changes.\n\nParameters:\n * None\n\nReturns:\n * The Yabai object",
        "name": "start",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The Yabai object"
        ],
        "signature": "Yabai:start() -> self",
        "stripped_doc": [
          "Builds the modal, creates the menubar item and starts watching for Space changes.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:stop() -> self",
        "desc": "Tears down the modal, the panel, the menubar item, the watcher, the chooser and anything still in flight.",
        "doc": "Tears down the modal, the panel, the menubar item, the watcher, the chooser and anything still in flight.\n\nParameters:\n * None\n\nReturns:\n * The Yabai object\n\nAny hotkeys bound with `Yabai:bindHotkeys()` stay bound; rebind or delete them separately if you want them gone.",
        "name": "stop",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "Any hotkeys bound with `Yabai:bindHotkeys()` stay bound; rebind or delete them separately if you want them gone."
        ],
        "signature": "Yabai:stop() -> self",
        "stripped_doc": [
          "Tears down the modal, the panel, the menubar item, the watcher, the chooser and anything still in flight.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:status() -> table",
        "desc": "Reports what the Spoon currently thinks the world looks like.",
        "doc": "Reports what the Spoon currently thinks the world looks like.\n\nParameters:\n * None\n\nReturns:\n * A table carrying `running`, `yabai`, `modalActive`, `level`, `chooserVisible`, `verb`, `step`, `menubar` and `pendingTasks`\n\nFor prodding from the Console. `hs.inspect(spoon.Yabai:status())` is the quickest way to tell whether yabai was found and whether a flow is half-finished. `verb` and `step` read the same whichever front end put them there.",
        "name": "status",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * A table carrying `running`, `yabai`, `modalActive`, `level`, `chooserVisible`, `verb`, `step`, `menubar` and `pendingTasks`",
          "",
          "For prodding from the Console. `hs.inspect(spoon.Yabai:status())` is the quickest way to tell whether yabai was found and whether a flow is half-finished. `verb` and `step` read the same whichever front end put them there."
        ],
        "signature": "Yabai:status() -> table",
        "stripped_doc": [
          "Reports what the Spoon currently thinks the world looks like.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:bindHotkeys(mapping) -> self",
        "desc": "Binds the hotkeys this Spoon offers.",
        "doc": "Binds the hotkeys this Spoon offers.\n\nParameters:\n * mapping - A table containing hotkey modifier/key details for the following items:\n   * modal - Enter the modal at the verb list; the leader for the whole grammar\n   * show - Open the chooser at the verb list, skipping the modal\n   * hide - Leave the modal and close the chooser, abandoning any half-finished flow\n\nReturns:\n * The Yabai object\n\nFor example: `spoon.Yabai:bindHotkeys({ modal = { { \"cmd\", \"alt\", \"ctrl\" }, \"Y\" } })`\n\nOne leader is enough: everything else is a letter inside the modal, and `/` reaches the chooser from any level, so binding `show` as well is optional. Every verb is also a method, so a favourite one can be given its own key directly: `hs.hotkey.bind({ \"cmd\", \"alt\", \"ctrl\" }, \"N\", function() spoon.Yabai:createSpaceOnDisplay(1) end)`.",
        "name": "bindHotkeys",
        "notes": [],
        "parameters": [
          " * mapping - A table containing hotkey modifier/key details for the following items:",
          "   * modal - Enter the modal at the verb list; the leader for the whole grammar",
          "   * show - Open the chooser at the verb list, skipping the modal",
          "   * hide - Leave the modal and close the chooser, abandoning any half-finished flow",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "For example: `spoon.Yabai:bindHotkeys({ modal = { { \"cmd\", \"alt\", \"ctrl\" }, \"Y\" } })`",
          "",
          "One leader is enough: everything else is a letter inside the modal, and `/` reaches the chooser from any level, so binding `show` as well is optional. Every verb is also a method, so a favourite one can be given its own key directly: `hs.hotkey.bind({ \"cmd\", \"alt\", \"ctrl\" }, \"N\", function() spoon.Yabai:createSpaceOnDisplay(1) end)`."
        ],
        "signature": "Yabai:bindHotkeys(mapping) -> self",
        "stripped_doc": [
          "Binds the hotkeys this Spoon offers.",
          ""
        ],
        "type": "Method"
      }
    ],
    "Variable": [
      {
        "def": "Yabai.logger",
        "desc": "Logger object used within the Spoon. Can be accessed to set the default log level for the messages coming from the Spoon.",
        "doc": "Logger object used within the Spoon. Can be accessed to set the default log level for the messages coming from the Spoon.",
        "name": "logger",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.logger",
        "stripped_doc": [
          "Logger object used within the Spoon. Can be accessed to set the default log level for the messages coming from the Spoon."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.chooserRows",
        "desc": "How many rows the chooser shows at once. Defaults to `10`.",
        "doc": "How many rows the chooser shows at once. Defaults to `10`.",
        "name": "chooserRows",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.chooserRows",
        "stripped_doc": [
          "How many rows the chooser shows at once. Defaults to `10`."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.chooserWidth",
        "desc": "Chooser width as a percentage of the screen. Defaults to `40`.",
        "doc": "Chooser width as a percentage of the screen. Defaults to `40`.",
        "name": "chooserWidth",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.chooserWidth",
        "stripped_doc": [
          "Chooser width as a percentage of the screen. Defaults to `40`."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.showInMenubar",
        "desc": "Whether to place an item in the menubar. Defaults to `true`.",
        "doc": "Whether to place an item in the menubar. Defaults to `true`.\n\nThe item is a button rather than a menu: clicking it opens the same chooser the hotkey does, so there is one way to choose a verb and not two.",
        "name": "showInMenubar",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.showInMenubar",
        "stripped_doc": [
          "Whether to place an item in the menubar. Defaults to `true`.",
          "",
          "The item is a button rather than a menu: clicking it opens the same chooser the hotkey does, so there is one way to choose a verb and not two."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.menubarTitle",
        "desc": "Glyph shown in the menubar, with the current Space count appended. Defaults to a squared plus.",
        "doc": "Glyph shown in the menubar, with the current Space count appended. Defaults to a squared plus.",
        "name": "menubarTitle",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.menubarTitle",
        "stripped_doc": [
          "Glyph shown in the menubar, with the current Space count appended. Defaults to a squared plus."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.confirmWhenWindows",
        "desc": "Whether to confirm before deleting a Space that still holds managed windows. Defaults to `true`.",
        "doc": "Whether to confirm before deleting a Space that still holds managed windows. Defaults to `true`.\n\nAn empty Space is deleted without a prompt. Deleting one that is not empty does not close its windows, it relocates them to another Space, which is recoverable but disorienting enough to be worth a keypress.\n\nScoped to deletion alone. Moving, reordering and focusing are all reversible by doing them again, so none of them prompt.",
        "name": "confirmWhenWindows",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.confirmWhenWindows",
        "stripped_doc": [
          "Whether to confirm before deleting a Space that still holds managed windows. Defaults to `true`.",
          "",
          "An empty Space is deleted without a prompt. Deleting one that is not empty does not close its windows, it relocates them to another Space, which is recoverable but disorienting enough to be worth a keypress.",
          "",
          "Scoped to deletion alone. Moving, reordering and focusing are all reversible by doing them again, so none of them prompt."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.countFloating",
        "desc": "Whether floating windows count towards a Space's total. Defaults to `false`.",
        "doc": "Whether floating windows count towards a Space's total. Defaults to `false`.\n\nFalse matches what yabai calls a managed window. It also excludes sticky windows for free, since a sticky window is always floating, and a sticky window is on every Space rather than the one being counted.\n\nIgnored on the `hs.spaces` fallback, which cannot tell a floating window from a tiled one and so always reports every window on the Space.",
        "name": "countFloating",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.countFloating",
        "stripped_doc": [
          "Whether floating windows count towards a Space's total. Defaults to `false`.",
          "",
          "False matches what yabai calls a managed window. It also excludes sticky windows for free, since a sticky window is always floating, and a sticky window is on every Space rather than the one being counted.",
          "",
          "Ignored on the `hs.spaces` fallback, which cannot tell a floating window from a tiled one and so always reports every window on the Space."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.showMinimized",
        "desc": "Whether minimized windows appear in the window lists. Defaults to `false`.",
        "doc": "Whether minimized windows appear in the window lists. Defaults to `false`.\n\nA minimized window has no Space in any useful sense: it sits in the Dock, and moving it somewhere only decides where it will reappear. `DeminimizeWindow` is the Spoon for that, so they are left out here by default.",
        "name": "showMinimized",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.showMinimized",
        "stripped_doc": [
          "Whether minimized windows appear in the window lists. Defaults to `false`.",
          "",
          "A minimized window has no Space in any useful sense: it sits in the Dock, and moving it somewhere only decides where it will reappear. `DeminimizeWindow` is the Spoon for that, so they are left out here by default."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.useYabai",
        "desc": "Whether to use yabai when it is installed. Defaults to `true`.",
        "doc": "Whether to use yabai when it is installed. Defaults to `true`.\n\nSet to `false` to exercise the `hs.spaces` fallbacks on a machine that has yabai.",
        "name": "useYabai",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.useYabai",
        "stripped_doc": [
          "Whether to use yabai when it is installed. Defaults to `true`.",
          "",
          "Set to `false` to exercise the `hs.spaces` fallbacks on a machine that has yabai."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.yabaiPath",
        "desc": "Absolute path to the yabai binary, or `nil` to search the usual install prefixes. Defaults to `nil`.",
        "doc": "Absolute path to the yabai binary, or `nil` to search the usual install prefixes. Defaults to `nil`.",
        "name": "yabaiPath",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.yabaiPath",
        "stripped_doc": [
          "Absolute path to the yabai binary, or `nil` to search the usual install prefixes. Defaults to `nil`."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.yabaiTimeout",
        "desc": "Seconds to wait for a yabai command before giving up on it. Defaults to `2`.",
        "doc": "Seconds to wait for a yabai command before giving up on it. Defaults to `2`.",
        "name": "yabaiTimeout",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.yabaiTimeout",
        "stripped_doc": [
          "Seconds to wait for a yabai command before giving up on it. Defaults to `2`."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.verifyTimeout",
        "desc": "Seconds to keep re-reading the world while waiting for a verb to take effect. Defaults to `1.5`.",
        "doc": "Seconds to keep re-reading the world while waiting for a verb to take effect. Defaults to `1.5`.\n\nNothing here trusts an exit code. A window manager acknowledges a command long before macOS finishes animating it, so every verb polls until it observes the change or this runs out.",
        "name": "verifyTimeout",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.verifyTimeout",
        "stripped_doc": [
          "Seconds to keep re-reading the world while waiting for a verb to take effect. Defaults to `1.5`.",
          "",
          "Nothing here trusts an exit code. A window manager acknowledges a command long before macOS finishes animating it, so every verb polls until it observes the change or this runs out."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.verifyInterval",
        "desc": "Seconds between those re-reads. Defaults to `0.15`.",
        "doc": "Seconds between those re-reads. Defaults to `0.15`.",
        "name": "verifyInterval",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.verifyInterval",
        "stripped_doc": [
          "Seconds between those re-reads. Defaults to `0.15`."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.hintKeys",
        "desc": "Letters handed to rows that have no natural key of their own, in the order they are handed out. Defaults to the home row first.",
        "doc": "Letters handed to rows that have no natural key of their own, in the order they are handed out. Defaults to the home row first.\n\nSpaces, displays and positions key off the number Mission Control already gives them, so this is mostly what windows are labelled with.",
        "name": "hintKeys",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.hintKeys",
        "stripped_doc": [
          "Letters handed to rows that have no natural key of their own, in the order they are handed out. Defaults to the home row first.",
          "",
          "Spaces, displays and positions key off the number Mission Control already gives them, so this is mostly what windows are labelled with."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.panelDelay",
        "desc": "Seconds the modal waits before drawing the panel. Defaults to `0.2`.",
        "doc": "Seconds the modal waits before drawing the panel. Defaults to `0.2`.\n\nMirrors which-key's own delay, and for the same reason: a sequence typed from memory finishes inside it and never puts anything on screen, while a pause is read as not knowing what comes next and is answered with the list.",
        "name": "panelDelay",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.panelDelay",
        "stripped_doc": [
          "Seconds the modal waits before drawing the panel. Defaults to `0.2`.",
          "",
          "Mirrors which-key's own delay, and for the same reason: a sequence typed from memory finishes inside it and never puts anything on screen, while a pause is read as not knowing what comes next and is answered with the list."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.modalTimeout",
        "desc": "Seconds of inactivity after which the modal leaves by itself. Defaults to `5`.",
        "doc": "Seconds of inactivity after which the modal leaves by itself. Defaults to `5`.\n\nThe modal swallows every letter and digit while it is up, so a forgotten one is a keyboard that appears to have stopped working. This is the backstop for walking away mid-sequence.",
        "name": "modalTimeout",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.modalTimeout",
        "stripped_doc": [
          "Seconds of inactivity after which the modal leaves by itself. Defaults to `5`.",
          "",
          "The modal swallows every letter and digit while it is up, so a forgotten one is a keyboard that appears to have stopped working. This is the backstop for walking away mid-sequence."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.panelRows",
        "desc": "Rows per column on the panel, before a second column is started. Defaults to `12`.",
        "doc": "Rows per column on the panel, before a second column is started. Defaults to `12`.\n\nFour columns is the ceiling. Anything past that is dropped from the panel, counted in the footer, and still reachable through `/`.",
        "name": "panelRows",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.panelRows",
        "stripped_doc": [
          "Rows per column on the panel, before a second column is started. Defaults to `12`.",
          "",
          "Four columns is the ceiling. Anything past that is dropped from the panel, counted in the footer, and still reachable through `/`."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.panelStyle",
        "desc": "Appearance of the panel, shaped like `hs.alert.defaultStyle` so it sits alongside the alerts the verbs report through.",
        "doc": "Appearance of the panel, shaped like `hs.alert.defaultStyle` so it sits alongside the alerts the verbs report through.\n\nRead at draw time, so a change from the Console shows up on the next level.",
        "name": "panelStyle",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.panelStyle",
        "stripped_doc": [
          "Appearance of the panel, shaped like `hs.alert.defaultStyle` so it sits alongside the alerts the verbs report through.",
          "",
          "Read at draw time, so a change from the Console shows up on the next level."
        ],
        "type": "Variable"
      }
    ],
    "desc": "A modal keyboard grammar for Mission Control: create, delete, reorder and focus Spaces, and move",
    "doc": "A modal keyboard grammar for Mission Control: create, delete, reorder and focus Spaces, and move\nwindows between Spaces and displays.\n\nThe grammar is vim's, verb before noun. A leader hotkey enters an `hs.hotkey.modal`, one keystroke\nnames the verb -- `f` focus, `d` delete, `m` move a window -- and one further keystroke names each\noperand that verb takes, so `f3` focuses Desktop 3 and `ma3` moves window `a` onto Space 3. A\nwhich-key style panel lists what the next keystroke can be, after a short delay, so a sequence typed\nat speed draws nothing at all.\n\nUnderneath it is a step stack rather than a list. The first level is the verb -- Create Space, Move\nWindow to Space -- and picking one pushes however many operands that verb needs, one level each.\nEscape pops a level and leaves from the top, so a wrong turn costs a keypress rather than a restart.\n\n`/` at any level hands the same half-finished operation to an `hs.chooser`, which is the same step\nstack rendered as a list. It is where a window with a long title is easier to find by typing than by\nhunting for its letter, and it is what the menubar item opens.\n\nPrefers `yabai` for reading, which reports every Space, window and display over a socket without\ndisturbing anything. Writing is a separate question. yabai can only mutate a Space through its\nscripting addition, which is an extra install and absent on plenty of machines otherwise running\nyabai fine, so every verb that has an `hs.spaces` equivalent falls back to it when the yabai command\nfails -- not merely when yabai is missing. The two are mixed deliberately: the fast reader is worth\nhaving even where it cannot be the writer.\n\nTwo verbs have no fallback at all, because nothing outside yabai can reorder a Space or send one to\nanother display. Where yabai cannot do them they stay listed, greyed and keyless, with the reason -- the\nsame treatment a Space macOS refuses to delete gets, so the panel always matches what is really on offer.\n\nWithout yabai entirely the Spoon still creates, deletes and focuses Spaces through `hs.spaces`, and\nstill lists windows through `hs.window.filter`. It is named for its preferred backend, not its only one.",
    "items": [
      {
        "def": "Yabai.chooserRows",
        "desc": "How many rows the chooser shows at once. Defaults to `10`.",
        "doc": "How many rows the chooser shows at once. Defaults to `10`.",
        "name": "chooserRows",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.chooserRows",
        "stripped_doc": [
          "How many rows the chooser shows at once. Defaults to `10`."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.chooserWidth",
        "desc": "Chooser width as a percentage of the screen. Defaults to `40`.",
        "doc": "Chooser width as a percentage of the screen. Defaults to `40`.",
        "name": "chooserWidth",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.chooserWidth",
        "stripped_doc": [
          "Chooser width as a percentage of the screen. Defaults to `40`."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.confirmWhenWindows",
        "desc": "Whether to confirm before deleting a Space that still holds managed windows. Defaults to `true`.",
        "doc": "Whether to confirm before deleting a Space that still holds managed windows. Defaults to `true`.\n\nAn empty Space is deleted without a prompt. Deleting one that is not empty does not close its windows, it relocates them to another Space, which is recoverable but disorienting enough to be worth a keypress.\n\nScoped to deletion alone. Moving, reordering and focusing are all reversible by doing them again, so none of them prompt.",
        "name": "confirmWhenWindows",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.confirmWhenWindows",
        "stripped_doc": [
          "Whether to confirm before deleting a Space that still holds managed windows. Defaults to `true`.",
          "",
          "An empty Space is deleted without a prompt. Deleting one that is not empty does not close its windows, it relocates them to another Space, which is recoverable but disorienting enough to be worth a keypress.",
          "",
          "Scoped to deletion alone. Moving, reordering and focusing are all reversible by doing them again, so none of them prompt."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.countFloating",
        "desc": "Whether floating windows count towards a Space's total. Defaults to `false`.",
        "doc": "Whether floating windows count towards a Space's total. Defaults to `false`.\n\nFalse matches what yabai calls a managed window. It also excludes sticky windows for free, since a sticky window is always floating, and a sticky window is on every Space rather than the one being counted.\n\nIgnored on the `hs.spaces` fallback, which cannot tell a floating window from a tiled one and so always reports every window on the Space.",
        "name": "countFloating",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.countFloating",
        "stripped_doc": [
          "Whether floating windows count towards a Space's total. Defaults to `false`.",
          "",
          "False matches what yabai calls a managed window. It also excludes sticky windows for free, since a sticky window is always floating, and a sticky window is on every Space rather than the one being counted.",
          "",
          "Ignored on the `hs.spaces` fallback, which cannot tell a floating window from a tiled one and so always reports every window on the Space."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.hintKeys",
        "desc": "Letters handed to rows that have no natural key of their own, in the order they are handed out. Defaults to the home row first.",
        "doc": "Letters handed to rows that have no natural key of their own, in the order they are handed out. Defaults to the home row first.\n\nSpaces, displays and positions key off the number Mission Control already gives them, so this is mostly what windows are labelled with.",
        "name": "hintKeys",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.hintKeys",
        "stripped_doc": [
          "Letters handed to rows that have no natural key of their own, in the order they are handed out. Defaults to the home row first.",
          "",
          "Spaces, displays and positions key off the number Mission Control already gives them, so this is mostly what windows are labelled with."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.logger",
        "desc": "Logger object used within the Spoon. Can be accessed to set the default log level for the messages coming from the Spoon.",
        "doc": "Logger object used within the Spoon. Can be accessed to set the default log level for the messages coming from the Spoon.",
        "name": "logger",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.logger",
        "stripped_doc": [
          "Logger object used within the Spoon. Can be accessed to set the default log level for the messages coming from the Spoon."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.menubarTitle",
        "desc": "Glyph shown in the menubar, with the current Space count appended. Defaults to a squared plus.",
        "doc": "Glyph shown in the menubar, with the current Space count appended. Defaults to a squared plus.",
        "name": "menubarTitle",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.menubarTitle",
        "stripped_doc": [
          "Glyph shown in the menubar, with the current Space count appended. Defaults to a squared plus."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.modalTimeout",
        "desc": "Seconds of inactivity after which the modal leaves by itself. Defaults to `5`.",
        "doc": "Seconds of inactivity after which the modal leaves by itself. Defaults to `5`.\n\nThe modal swallows every letter and digit while it is up, so a forgotten one is a keyboard that appears to have stopped working. This is the backstop for walking away mid-sequence.",
        "name": "modalTimeout",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.modalTimeout",
        "stripped_doc": [
          "Seconds of inactivity after which the modal leaves by itself. Defaults to `5`.",
          "",
          "The modal swallows every letter and digit while it is up, so a forgotten one is a keyboard that appears to have stopped working. This is the backstop for walking away mid-sequence."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.panelDelay",
        "desc": "Seconds the modal waits before drawing the panel. Defaults to `0.2`.",
        "doc": "Seconds the modal waits before drawing the panel. Defaults to `0.2`.\n\nMirrors which-key's own delay, and for the same reason: a sequence typed from memory finishes inside it and never puts anything on screen, while a pause is read as not knowing what comes next and is answered with the list.",
        "name": "panelDelay",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.panelDelay",
        "stripped_doc": [
          "Seconds the modal waits before drawing the panel. Defaults to `0.2`.",
          "",
          "Mirrors which-key's own delay, and for the same reason: a sequence typed from memory finishes inside it and never puts anything on screen, while a pause is read as not knowing what comes next and is answered with the list."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.panelRows",
        "desc": "Rows per column on the panel, before a second column is started. Defaults to `12`.",
        "doc": "Rows per column on the panel, before a second column is started. Defaults to `12`.\n\nFour columns is the ceiling. Anything past that is dropped from the panel, counted in the footer, and still reachable through `/`.",
        "name": "panelRows",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.panelRows",
        "stripped_doc": [
          "Rows per column on the panel, before a second column is started. Defaults to `12`.",
          "",
          "Four columns is the ceiling. Anything past that is dropped from the panel, counted in the footer, and still reachable through `/`."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.panelStyle",
        "desc": "Appearance of the panel, shaped like `hs.alert.defaultStyle` so it sits alongside the alerts the verbs report through.",
        "doc": "Appearance of the panel, shaped like `hs.alert.defaultStyle` so it sits alongside the alerts the verbs report through.\n\nRead at draw time, so a change from the Console shows up on the next level.",
        "name": "panelStyle",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.panelStyle",
        "stripped_doc": [
          "Appearance of the panel, shaped like `hs.alert.defaultStyle` so it sits alongside the alerts the verbs report through.",
          "",
          "Read at draw time, so a change from the Console shows up on the next level."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.showInMenubar",
        "desc": "Whether to place an item in the menubar. Defaults to `true`.",
        "doc": "Whether to place an item in the menubar. Defaults to `true`.\n\nThe item is a button rather than a menu: clicking it opens the same chooser the hotkey does, so there is one way to choose a verb and not two.",
        "name": "showInMenubar",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.showInMenubar",
        "stripped_doc": [
          "Whether to place an item in the menubar. Defaults to `true`.",
          "",
          "The item is a button rather than a menu: clicking it opens the same chooser the hotkey does, so there is one way to choose a verb and not two."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.showMinimized",
        "desc": "Whether minimized windows appear in the window lists. Defaults to `false`.",
        "doc": "Whether minimized windows appear in the window lists. Defaults to `false`.\n\nA minimized window has no Space in any useful sense: it sits in the Dock, and moving it somewhere only decides where it will reappear. `DeminimizeWindow` is the Spoon for that, so they are left out here by default.",
        "name": "showMinimized",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.showMinimized",
        "stripped_doc": [
          "Whether minimized windows appear in the window lists. Defaults to `false`.",
          "",
          "A minimized window has no Space in any useful sense: it sits in the Dock, and moving it somewhere only decides where it will reappear. `DeminimizeWindow` is the Spoon for that, so they are left out here by default."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.useYabai",
        "desc": "Whether to use yabai when it is installed. Defaults to `true`.",
        "doc": "Whether to use yabai when it is installed. Defaults to `true`.\n\nSet to `false` to exercise the `hs.spaces` fallbacks on a machine that has yabai.",
        "name": "useYabai",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.useYabai",
        "stripped_doc": [
          "Whether to use yabai when it is installed. Defaults to `true`.",
          "",
          "Set to `false` to exercise the `hs.spaces` fallbacks on a machine that has yabai."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.verifyInterval",
        "desc": "Seconds between those re-reads. Defaults to `0.15`.",
        "doc": "Seconds between those re-reads. Defaults to `0.15`.",
        "name": "verifyInterval",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.verifyInterval",
        "stripped_doc": [
          "Seconds between those re-reads. Defaults to `0.15`."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.verifyTimeout",
        "desc": "Seconds to keep re-reading the world while waiting for a verb to take effect. Defaults to `1.5`.",
        "doc": "Seconds to keep re-reading the world while waiting for a verb to take effect. Defaults to `1.5`.\n\nNothing here trusts an exit code. A window manager acknowledges a command long before macOS finishes animating it, so every verb polls until it observes the change or this runs out.",
        "name": "verifyTimeout",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.verifyTimeout",
        "stripped_doc": [
          "Seconds to keep re-reading the world while waiting for a verb to take effect. Defaults to `1.5`.",
          "",
          "Nothing here trusts an exit code. A window manager acknowledges a command long before macOS finishes animating it, so every verb polls until it observes the change or this runs out."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.yabaiPath",
        "desc": "Absolute path to the yabai binary, or `nil` to search the usual install prefixes. Defaults to `nil`.",
        "doc": "Absolute path to the yabai binary, or `nil` to search the usual install prefixes. Defaults to `nil`.",
        "name": "yabaiPath",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.yabaiPath",
        "stripped_doc": [
          "Absolute path to the yabai binary, or `nil` to search the usual install prefixes. Defaults to `nil`."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai.yabaiTimeout",
        "desc": "Seconds to wait for a yabai command before giving up on it. Defaults to `2`.",
        "doc": "Seconds to wait for a yabai command before giving up on it. Defaults to `2`.",
        "name": "yabaiTimeout",
        "notes": [],
        "parameters": [],
        "returns": [],
        "signature": "Yabai.yabaiTimeout",
        "stripped_doc": [
          "Seconds to wait for a yabai command before giving up on it. Defaults to `2`."
        ],
        "type": "Variable"
      },
      {
        "def": "Yabai:assignKeys(rows) -> table",
        "desc": "Gives every selectable row a key and returns the key-to-row map the modal dispatches through.",
        "doc": "Gives every selectable row a key and returns the key-to-row map the modal dispatches through.\n\nParameters:\n * rows - A list of choice tables, as the operand builders and `Yabai:verbChoices()` produce\n\nReturns:\n * A table mapping each assigned key to its row\n\nA row's own `hint` wins where it is still free -- the Space index, the display number, the verb's mnemonic -- and everything else takes the next unused letter from `Yabai.hintKeys`.\n\nA row that cannot be chosen is left keyless rather than skipped over, so a Space in the middle of the list that macOS refuses to delete does not shift the digits of the ones after it. The keys on the panel keep matching the numbers in Mission Control.",
        "name": "assignKeys",
        "notes": [],
        "parameters": [
          " * rows - A list of choice tables, as the operand builders and `Yabai:verbChoices()` produce",
          ""
        ],
        "returns": [
          " * A table mapping each assigned key to its row",
          "",
          "A row's own `hint` wins where it is still free -- the Space index, the display number, the verb's mnemonic -- and everything else takes the next unused letter from `Yabai.hintKeys`.",
          "",
          "A row that cannot be chosen is left keyless rather than skipped over, so a Space in the middle of the list that macOS refuses to delete does not shift the digits of the ones after it. The keys on the panel keep matching the numbers in Mission Control."
        ],
        "signature": "Yabai:assignKeys(rows) -> table",
        "stripped_doc": [
          "Gives every selectable row a key and returns the key-to-row map the modal dispatches through.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:back() -> self",
        "desc": "Steps one level back, dropping the pick made on the level being left.",
        "doc": "Steps one level back, dropping the pick made on the level being left.\n\nParameters:\n * None\n\nReturns:\n * The Yabai object\n\nWhat Escape, Delete and the `← Back` row all call, in either front end. On the first operand level it returns to the verb list; at the verb list there is nothing left to pop, so the chooser dismisses and the modal leaves.",
        "name": "back",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "What Escape, Delete and the `← Back` row all call, in either front end. On the first operand level it returns to the verb list; at the verb list there is nothing left to pop, so the chooser dismisses and the modal leaves."
        ],
        "signature": "Yabai:back() -> self",
        "stripped_doc": [
          "Steps one level back, dropping the pick made on the level being left.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:bindHotkeys(mapping) -> self",
        "desc": "Binds the hotkeys this Spoon offers.",
        "doc": "Binds the hotkeys this Spoon offers.\n\nParameters:\n * mapping - A table containing hotkey modifier/key details for the following items:\n   * modal - Enter the modal at the verb list; the leader for the whole grammar\n   * show - Open the chooser at the verb list, skipping the modal\n   * hide - Leave the modal and close the chooser, abandoning any half-finished flow\n\nReturns:\n * The Yabai object\n\nFor example: `spoon.Yabai:bindHotkeys({ modal = { { \"cmd\", \"alt\", \"ctrl\" }, \"Y\" } })`\n\nOne leader is enough: everything else is a letter inside the modal, and `/` reaches the chooser from any level, so binding `show` as well is optional. Every verb is also a method, so a favourite one can be given its own key directly: `hs.hotkey.bind({ \"cmd\", \"alt\", \"ctrl\" }, \"N\", function() spoon.Yabai:createSpaceOnDisplay(1) end)`.",
        "name": "bindHotkeys",
        "notes": [],
        "parameters": [
          " * mapping - A table containing hotkey modifier/key details for the following items:",
          "   * modal - Enter the modal at the verb list; the leader for the whole grammar",
          "   * show - Open the chooser at the verb list, skipping the modal",
          "   * hide - Leave the modal and close the chooser, abandoning any half-finished flow",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "For example: `spoon.Yabai:bindHotkeys({ modal = { { \"cmd\", \"alt\", \"ctrl\" }, \"Y\" } })`",
          "",
          "One leader is enough: everything else is a letter inside the modal, and `/` reaches the chooser from any level, so binding `show` as well is optional. Every verb is also a method, so a favourite one can be given its own key directly: `hs.hotkey.bind({ \"cmd\", \"alt\", \"ctrl\" }, \"N\", function() spoon.Yabai:createSpaceOnDisplay(1) end)`."
        ],
        "signature": "Yabai:bindHotkeys(mapping) -> self",
        "stripped_doc": [
          "Binds the hotkeys this Spoon offers.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:choicesFor(entries) -> table",
        "desc": "Renders a Space model as chooser rows for the delete screen, blocked Spaces included.",
        "doc": "Renders a Space model as chooser rows for the delete screen, blocked Spaces included.\n\nParameters:\n * entries - A Space model, as `Yabai:listSpaces()` produces\n\nReturns:\n * A list of `hs.chooser` choice tables\n\nA Space macOS refuses to remove is rendered with its reason and made unselectable rather than dropped, so the list still matches what Mission Control shows.",
        "name": "choicesFor",
        "notes": [],
        "parameters": [
          " * entries - A Space model, as `Yabai:listSpaces()` produces",
          ""
        ],
        "returns": [
          " * A list of `hs.chooser` choice tables",
          "",
          "A Space macOS refuses to remove is rendered with its reason and made unselectable rather than dropped, so the list still matches what Mission Control shows."
        ],
        "signature": "Yabai:choicesFor(entries) -> table",
        "stripped_doc": [
          "Renders a Space model as chooser rows for the delete screen, blocked Spaces included.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:createSpaceOnDisplay(displayIndex) -> self",
        "desc": "Adds a Space to the end of one display.",
        "doc": "Adds a Space to the end of one display.\n\nParameters:\n * displayIndex - The yabai display number, as reported by `Yabai:listDisplays()`\n\nReturns:\n * The Yabai object\n\nmacOS always appends: neither backend can insert a Space at a chosen position, so the new Space lands last and `Yabai:moveSpaceToPosition()` is what puts it somewhere else.",
        "name": "createSpaceOnDisplay",
        "notes": [],
        "parameters": [
          " * displayIndex - The yabai display number, as reported by `Yabai:listDisplays()`",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "macOS always appends: neither backend can insert a Space at a chosen position, so the new Space lands last and `Yabai:moveSpaceToPosition()` is what puts it somewhere else."
        ],
        "signature": "Yabai:createSpaceOnDisplay(displayIndex) -> self",
        "stripped_doc": [
          "Adds a Space to the end of one display.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:deleteById(id) -> self",
        "desc": "Deletes one Space, named by its stable Space id.",
        "doc": "Deletes one Space, named by its stable Space id.\n\nParameters:\n * id - The Space id, as reported by `Yabai:listSpaces()` or `hs.spaces`\n\nReturns:\n * The Yabai object\n\nRe-reads every Space before acting. The id the chooser handed back is stable, but the Mission Control position both backends select on is not, and the list may be seconds old by the time a choice is made, so the position is resolved from the id here and the Space is re-checked for having become active or last-on-its-screen in the meantime.\n\nOne Space per invocation, deliberately. Those positions renumber the moment a Space goes away, so a batch loop is a loop whose target drifts under it.",
        "name": "deleteById",
        "notes": [],
        "parameters": [
          " * id - The Space id, as reported by `Yabai:listSpaces()` or `hs.spaces`",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "Re-reads every Space before acting. The id the chooser handed back is stable, but the Mission Control position both backends select on is not, and the list may be seconds old by the time a choice is made, so the position is resolved from the id here and the Space is re-checked for having become active or last-on-its-screen in the meantime.",
          "",
          "One Space per invocation, deliberately. Those positions renumber the moment a Space goes away, so a batch loop is a loop whose target drifts under it."
        ],
        "signature": "Yabai:deleteById(id) -> self",
        "stripped_doc": [
          "Deletes one Space, named by its stable Space id.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:enterModal() -> self",
        "desc": "Enters the modal at the verb list.",
        "doc": "Enters the modal at the verb list.\n\nParameters:\n * None\n\nReturns:\n * The Yabai object\n\nWhat the leader hotkey is bound to. Any half-finished flow is abandoned, so this always starts from the verb list, and every letter and digit belongs to the modal until it is left: by finishing a verb, by Escape at the verb list, or by `Yabai.modalTimeout` seconds of doing nothing.",
        "name": "enterModal",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "What the leader hotkey is bound to. Any half-finished flow is abandoned, so this always starts from the verb list, and every letter and digit belongs to the modal until it is left: by finishing a verb, by Escape at the verb list, or by `Yabai.modalTimeout` seconds of doing nothing."
        ],
        "signature": "Yabai:enterModal() -> self",
        "stripped_doc": [
          "Enters the modal at the verb list.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:exitModal() -> self",
        "desc": "Leaves the modal and takes the panel down.",
        "doc": "Leaves the modal and takes the panel down.\n\nParameters:\n * None\n\nReturns:\n * The Yabai object\n\nAny half-finished flow is left alone rather than dropped, which is what lets `Yabai:searchHere()` hand it to the chooser mid-verb.",
        "name": "exitModal",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "Any half-finished flow is left alone rather than dropped, which is what lets `Yabai:searchHere()` hand it to the chooser mid-verb."
        ],
        "signature": "Yabai:exitModal() -> self",
        "stripped_doc": [
          "Leaves the modal and takes the panel down.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:focusSpaceById(id) -> self",
        "desc": "Switches to one Space, named by its stable Space id.",
        "doc": "Switches to one Space, named by its stable Space id.\n\nParameters:\n * id - The Space id, as reported by `Yabai:listSpaces()` or `hs.spaces`\n\nReturns:\n * The Yabai object\n\nThe one verb that needs nothing special from yabai: `space --focus` goes over the plain socket, so this works on a machine with System Integrity Protection fully enabled, and `hs.spaces.gotoSpace` stands behind it regardless.",
        "name": "focusSpaceById",
        "notes": [],
        "parameters": [
          " * id - The Space id, as reported by `Yabai:listSpaces()` or `hs.spaces`",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "The one verb that needs nothing special from yabai: `space --focus` goes over the plain socket, so this works on a machine with System Integrity Protection fully enabled, and `hs.spaces.gotoSpace` stands behind it regardless."
        ],
        "signature": "Yabai:focusSpaceById(id) -> self",
        "stripped_doc": [
          "Switches to one Space, named by its stable Space id.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:focusWindowById(winId) -> self",
        "desc": "Focuses one window, following it onto whatever Space it is on.",
        "doc": "Focuses one window, following it onto whatever Space it is on.\n\nParameters:\n * winId - The window id, as reported by `Yabai:listWindows()` or `hs.window:id()`\n\nReturns:\n * The Yabai object\n\nNeeds nothing from yabai's scripting addition, and `hs.window:focus()` stands behind it, so this is the one window verb that works everywhere.",
        "name": "focusWindowById",
        "notes": [],
        "parameters": [
          " * winId - The window id, as reported by `Yabai:listWindows()` or `hs.window:id()`",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "Needs nothing from yabai's scripting addition, and `hs.window:focus()` stands behind it, so this is the one window verb that works everywhere."
        ],
        "signature": "Yabai:focusWindowById(winId) -> self",
        "stripped_doc": [
          "Focuses one window, following it onto whatever Space it is on.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:hide() -> self",
        "desc": "Closes the chooser, abandoning any half-finished flow.",
        "doc": "Closes the chooser, abandoning any half-finished flow.\n\nParameters:\n * None\n\nReturns:\n * The Yabai object",
        "name": "hide",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The Yabai object"
        ],
        "signature": "Yabai:hide() -> self",
        "stripped_doc": [
          "Closes the chooser, abandoning any half-finished flow.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:keymap() -> table",
        "desc": "Reports the keys the level currently on the panel answers to.",
        "doc": "Reports the keys the level currently on the panel answers to.\n\nParameters:\n * None\n\nReturns:\n * A table mapping each live key to the row it would choose, empty outside the modal\n\nFor prodding from the Console: `hs.inspect(spoon.Yabai:keymap())` is the quickest way to see what a level assigned without reading the panel off the screen.",
        "name": "keymap",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * A table mapping each live key to the row it would choose, empty outside the modal",
          "",
          "For prodding from the Console: `hs.inspect(spoon.Yabai:keymap())` is the quickest way to see what a level assigned without reading the panel off the screen."
        ],
        "signature": "Yabai:keymap() -> table",
        "stripped_doc": [
          "Reports the keys the level currently on the panel answers to.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:listDisplays(done) -> self",
        "desc": "Lists the attached displays, with the number of Spaces on each.",
        "doc": "Lists the attached displays, with the number of Spaces on each.\n\nParameters:\n * done - Called as `done(entries, err)`. `entries` is an array of tables carrying `index`, `name`, `spaceCount`, `isActive` and `screen`\n\nReturns:\n * The Yabai object\n\n`index` is yabai's display number, which is what every yabai display selector wants, and `screen` is the matching `hs.screen` for the fallbacks, which speak in screen objects instead.",
        "name": "listDisplays",
        "notes": [],
        "parameters": [
          " * done - Called as `done(entries, err)`. `entries` is an array of tables carrying `index`, `name`, `spaceCount`, `isActive` and `screen`",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "`index` is yabai's display number, which is what every yabai display selector wants, and `screen` is the matching `hs.screen` for the fallbacks, which speak in screen objects instead."
        ],
        "signature": "Yabai:listDisplays(done) -> self",
        "stripped_doc": [
          "Lists the attached displays, with the number of Spaces on each.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:listSpaces(done) -> self",
        "desc": "Builds the current Space model and hands it to a callback.",
        "doc": "Builds the current Space model and hands it to a callback.\n\nParameters:\n * done - Called as `done(entries, err)`. `entries` is an array of tables carrying `id`, `index`, `label`, `screenName`, `displayIndex`, `managedCount`, `isActive`, `isFullscreen` and `blockedReason`, or nil and a message if the world could not be read\n\nReturns:\n * The Yabai object\n\nAsynchronous because the yabai path is: three socket queries, none of which may block the runloop. Exposed for prodding from the Console, where `hs.inspect` on the result is the fastest way to see what the chooser is about to show.",
        "name": "listSpaces",
        "notes": [],
        "parameters": [
          " * done - Called as `done(entries, err)`. `entries` is an array of tables carrying `id`, `index`, `label`, `screenName`, `displayIndex`, `managedCount`, `isActive`, `isFullscreen` and `blockedReason`, or nil and a message if the world could not be read",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "Asynchronous because the yabai path is: three socket queries, none of which may block the runloop. Exposed for prodding from the Console, where `hs.inspect` on the result is the fastest way to see what the chooser is about to show."
        ],
        "signature": "Yabai:listSpaces(done) -> self",
        "stripped_doc": [
          "Builds the current Space model and hands it to a callback.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:listWindows(done) -> self",
        "desc": "Lists every window that is not our own, with the Space it is on.",
        "doc": "Lists every window that is not our own, with the Space it is on.\n\nParameters:\n * done - Called as `done(entries, err)`. `entries` is an array of tables carrying `winId`, `appName`, `title`, `spaceId`, `spaceIndex`, `spaceName`, `displayIndex` and `minimized`\n\nReturns:\n * The Yabai object\n\nOwn windows are excluded by pid rather than by application name. `hs.window:application()` logs from Hammerspoon's C layer and returns nil transiently even for a live process, so a guard written on it fails open; the pid a window already carries cannot.\n\nMinimized windows are left out unless `Yabai.showMinimized` is set. They have no Space in any useful sense, and `DeminimizeWindow` is the Spoon that deals with them.",
        "name": "listWindows",
        "notes": [],
        "parameters": [
          " * done - Called as `done(entries, err)`. `entries` is an array of tables carrying `winId`, `appName`, `title`, `spaceId`, `spaceIndex`, `spaceName`, `displayIndex` and `minimized`",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "Own windows are excluded by pid rather than by application name. `hs.window:application()` logs from Hammerspoon's C layer and returns nil transiently even for a live process, so a guard written on it fails open; the pid a window already carries cannot.",
          "",
          "Minimized windows are left out unless `Yabai.showMinimized` is set. They have no Space in any useful sense, and `DeminimizeWindow` is the Spoon that deals with them."
        ],
        "signature": "Yabai:listWindows(done) -> self",
        "stripped_doc": [
          "Lists every window that is not our own, with the Space it is on.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:moveSpaceToDisplay(id, displayIndex) -> self",
        "desc": "Sends a whole Space, and everything on it, to another display.",
        "doc": "Sends a whole Space, and everything on it, to another display.\n\nParameters:\n * id - The Space id to send\n * displayIndex - The yabai display number to send it to\n\nReturns:\n * The Yabai object\n\nyabai only, for the same reason as `Yabai:moveSpaceToPosition()`: `hs.spaces` cannot move a Space between displays at all.",
        "name": "moveSpaceToDisplay",
        "notes": [],
        "parameters": [
          " * id - The Space id to send",
          " * displayIndex - The yabai display number to send it to",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "yabai only, for the same reason as `Yabai:moveSpaceToPosition()`: `hs.spaces` cannot move a Space between displays at all."
        ],
        "signature": "Yabai:moveSpaceToDisplay(id, displayIndex) -> self",
        "stripped_doc": [
          "Sends a whole Space, and everything on it, to another display.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:moveSpaceToPosition(id, position) -> self",
        "desc": "Moves a Space to another Mission Control position on the display it is already on.",
        "doc": "Moves a Space to another Mission Control position on the display it is already on.\n\nParameters:\n * id - The Space id to move\n * position - The Mission Control position to move it to, counted across all displays as yabai counts them\n\nReturns:\n * The Yabai object\n\nyabai only. `hs.spaces` can create, remove and switch Spaces but has no way to reorder them, so where yabai cannot do this nothing can, and the failure says so rather than pretending a fallback was tried.",
        "name": "moveSpaceToPosition",
        "notes": [],
        "parameters": [
          " * id - The Space id to move",
          " * position - The Mission Control position to move it to, counted across all displays as yabai counts them",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "yabai only. `hs.spaces` can create, remove and switch Spaces but has no way to reorder them, so where yabai cannot do this nothing can, and the failure says so rather than pretending a fallback was tried."
        ],
        "signature": "Yabai:moveSpaceToPosition(id, position) -> self",
        "stripped_doc": [
          "Moves a Space to another Mission Control position on the display it is already on.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:moveWindowToDisplay(winId, displayIndex) -> self",
        "desc": "Sends one window to another display.",
        "doc": "Sends one window to another display.\n\nParameters:\n * winId - The window id to send\n * displayIndex - The yabai display number to send it to\n\nReturns:\n * The Yabai object\n\n`hs.window:moveToScreen()` stands behind yabai here and genuinely works, because moving a window between displays does not cross a Space and so needs no scripting addition.",
        "name": "moveWindowToDisplay",
        "notes": [],
        "parameters": [
          " * winId - The window id to send",
          " * displayIndex - The yabai display number to send it to",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "`hs.window:moveToScreen()` stands behind yabai here and genuinely works, because moving a window between displays does not cross a Space and so needs no scripting addition."
        ],
        "signature": "Yabai:moveWindowToDisplay(winId, displayIndex) -> self",
        "stripped_doc": [
          "Sends one window to another display.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:moveWindowToSpace(winId, spaceId) -> self",
        "desc": "Moves one window to another Space.",
        "doc": "Moves one window to another Space.\n\nParameters:\n * winId - The window id to move\n * spaceId - The stable Space id to move it to\n\nReturns:\n * The Yabai object\n\nyabai is the real path here. `hs.spaces.moveWindowToSpace()` is tried behind it and reported honestly, but it has been a silent no-op since macOS 15 -- it returns success and moves nothing -- which is why the result is polled rather than believed. `SummonWindow` carries a third rung that borrows the mouse pointer to drag the window across in Mission Control; it is deliberately not duplicated here.",
        "name": "moveWindowToSpace",
        "notes": [],
        "parameters": [
          " * winId - The window id to move",
          " * spaceId - The stable Space id to move it to",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "yabai is the real path here. `hs.spaces.moveWindowToSpace()` is tried behind it and reported honestly, but it has been a silent no-op since macOS 15 -- it returns success and moves nothing -- which is why the result is polled rather than believed. `SummonWindow` carries a third rung that borrows the mouse pointer to drag the window across in Mission Control; it is deliberately not duplicated here."
        ],
        "signature": "Yabai:moveWindowToSpace(winId, spaceId) -> self",
        "stripped_doc": [
          "Moves one window to another Space.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:onKey(char) -> self",
        "desc": "Routes one keystroke against the level currently on the panel.",
        "doc": "Routes one keystroke against the level currently on the panel.\n\nParameters:\n * char - The single character the modal was handed\n\nReturns:\n * The Yabai object\n\nEvery letter and digit reaches this, bound or not. One with nothing behind it is swallowed and reported in the panel's footer, because a modal that lets a stray keystroke through into the window underneath is worse than one that says no.",
        "name": "onKey",
        "notes": [],
        "parameters": [
          " * char - The single character the modal was handed",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "Every letter and digit reaches this, bound or not. One with nothing behind it is swallowed and reported in the panel's footer, because a modal that lets a stray keystroke through into the window underneath is worse than one that says no."
        ],
        "signature": "Yabai:onKey(char) -> self",
        "stripped_doc": [
          "Routes one keystroke against the level currently on the panel.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:searchHere() -> self",
        "desc": "Leaves the modal and reopens the same half-finished operation in the chooser.",
        "doc": "Leaves the modal and reopens the same half-finished operation in the chooser.\n\nParameters:\n * None\n\nReturns:\n * The Yabai object\n\nWhat `/` does. The two front ends push and pop the same flow, so the chooser opens on the level the modal was standing on with every pick so far intact, and a window that is easier to find by typing is found by typing.",
        "name": "searchHere",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "What `/` does. The two front ends push and pop the same flow, so the chooser opens on the level the modal was standing on with every pick so far intact, and a window that is easier to find by typing is found by typing."
        ],
        "signature": "Yabai:searchHere() -> self",
        "stripped_doc": [
          "Leaves the modal and reopens the same half-finished operation in the chooser.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:show() -> self",
        "desc": "Opens the chooser at the verb list.",
        "doc": "Opens the chooser at the verb list.\n\nParameters:\n * None\n\nReturns:\n * The Yabai object\n\nWhat the menubar item's click opens, and what `Yabai:searchHere()` falls back to. Any half-finished flow is abandoned, so this always starts from the top; `/` inside the modal is the way to open the chooser mid-verb instead.",
        "name": "show",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "What the menubar item's click opens, and what `Yabai:searchHere()` falls back to. Any half-finished flow is abandoned, so this always starts from the top; `/` inside the modal is the way to open the chooser mid-verb instead."
        ],
        "signature": "Yabai:show() -> self",
        "stripped_doc": [
          "Opens the chooser at the verb list.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:showLevel() -> self",
        "desc": "Rebuilds the panel for wherever the flow currently stands: the verb list, or one operand level.",
        "doc": "Rebuilds the panel for wherever the flow currently stands: the verb list, or one operand level.\n\nParameters:\n * None\n\nReturns:\n * The Yabai object\n\nThe modal's half of `Yabai:showStep()`, and the direct counterpart of `Yabai:showOperand()`. Both read the same flow and the same operand builders; only the rendering differs.",
        "name": "showLevel",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "The modal's half of `Yabai:showStep()`, and the direct counterpart of `Yabai:showOperand()`. Both read the same flow and the same operand builders; only the rendering differs."
        ],
        "signature": "Yabai:showLevel() -> self",
        "stripped_doc": [
          "Rebuilds the panel for wherever the flow currently stands: the verb list, or one operand level.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:start() -> self",
        "desc": "Builds the modal, creates the menubar item and starts watching for Space changes.",
        "doc": "Builds the modal, creates the menubar item and starts watching for Space changes.\n\nParameters:\n * None\n\nReturns:\n * The Yabai object",
        "name": "start",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The Yabai object"
        ],
        "signature": "Yabai:start() -> self",
        "stripped_doc": [
          "Builds the modal, creates the menubar item and starts watching for Space changes.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:status() -> table",
        "desc": "Reports what the Spoon currently thinks the world looks like.",
        "doc": "Reports what the Spoon currently thinks the world looks like.\n\nParameters:\n * None\n\nReturns:\n * A table carrying `running`, `yabai`, `modalActive`, `level`, `chooserVisible`, `verb`, `step`, `menubar` and `pendingTasks`\n\nFor prodding from the Console. `hs.inspect(spoon.Yabai:status())` is the quickest way to tell whether yabai was found and whether a flow is half-finished. `verb` and `step` read the same whichever front end put them there.",
        "name": "status",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * A table carrying `running`, `yabai`, `modalActive`, `level`, `chooserVisible`, `verb`, `step`, `menubar` and `pendingTasks`",
          "",
          "For prodding from the Console. `hs.inspect(spoon.Yabai:status())` is the quickest way to tell whether yabai was found and whether a flow is half-finished. `verb` and `step` read the same whichever front end put them there."
        ],
        "signature": "Yabai:status() -> table",
        "stripped_doc": [
          "Reports what the Spoon currently thinks the world looks like.",
          ""
        ],
        "type": "Method"
      },
      {
        "def": "Yabai:stop() -> self",
        "desc": "Tears down the modal, the panel, the menubar item, the watcher, the chooser and anything still in flight.",
        "doc": "Tears down the modal, the panel, the menubar item, the watcher, the chooser and anything still in flight.\n\nParameters:\n * None\n\nReturns:\n * The Yabai object\n\nAny hotkeys bound with `Yabai:bindHotkeys()` stay bound; rebind or delete them separately if you want them gone.",
        "name": "stop",
        "notes": [],
        "parameters": [
          " * None",
          ""
        ],
        "returns": [
          " * The Yabai object",
          "",
          "Any hotkeys bound with `Yabai:bindHotkeys()` stay bound; rebind or delete them separately if you want them gone."
        ],
        "signature": "Yabai:stop() -> self",
        "stripped_doc": [
          "Tears down the modal, the panel, the menubar item, the watcher, the chooser and anything still in flight.",
          ""
        ],
        "type": "Method"
      }
    ],
    "name": "Yabai",
    "stripped_doc": [],
    "submodules": [],
    "type": "Module"
  }
]
